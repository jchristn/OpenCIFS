# Changelog

## Unreleased

- Started the bounded Milestone 7 SMB1/CIFS codec foundation: `Smb1DialectStrings` defines `LANMAN1.0`, `LM1.2X002`, `LANMAN2.1`, and `NT LM 0.12` alongside the existing SMB 2.x bridge strings. New `Smb1SecurityMode` and `Smb1Capabilities` flags enums per MS-CIFS section 2.2.4.5.2. New `Smb1NegotiateResponse` codec carries the 17-word NT LM 0.12 / extended-security response shape (DialectIndex, SecurityMode, MaxMpxCount/MaxNumberVcs, MaxBufferSize/MaxRawSize, SessionKey, Capabilities, SystemTime, ServerTimeZoneMinutes, ServerGuid, optional SPNEGO security blob). Shared core suite pins extended-security round-trip plus rejection of truncated responses and non-extended-security responses. Coverage matrix SMB1/CIFS negotiate row lifted from `backlog` to `shared/shared/false/true` for the codec foundation. SMB1 stays disabled by default through `OpenCifsServerOptions.EnableSmb1`; real client/server selection of SMB1 dialects remains backlog.
- Extended `eng/run-nightly-interop.ps1` to compose four-dialect external interop runs (SMB 2.0.2, SMB 2.1, SMB 3.0.2, and the bounded SMB 3.1.1 preview) across the Python real-client, Samba Docker, and native Windows mapped-drive harnesses, plus copies the per-dialect `smb302` and `smb311` artifact subtrees into the nightly result. Fixed a pre-existing oversight where `smb302` was missing from the per-harness relative-path arrays. Updated the composed `encryption_status` to `bounded-smb302-plus-smb311-preview` and refreshed the notes block to describe the new SMB 3.1.1 lane plus remaining backlog (SMB 3.1.1 durable reconnect, AES-256 ciphers gated on Kerberos).
- Extended the Python `smbprotocol` real-client interop harness with an SMB 3.1.1 lane: `eng/run-real-client-interop.ps1` default `-Dialects` now includes `Smb311`, dialect-metadata helper carries `EnableSmb311Preview`, and the harness propagates `--enable-smb311-preview true` to `Sample.OpenCifsServer` on the Smb311 lane. `eng/real-client-smoke.py` now accepts `--dialect smb311`, returns `Dialects.SMB_3_1_1`, and treats SMB 3.1.1 as encryption-required so the smbprotocol client matches the bounded preview's encrypted session posture.
- Extended the native Windows mapped-drive interop harness with an SMB 3.1.1 lane: `eng/run-windows-client-interop.ps1` default `-Dialects` now includes `Smb311` and the dialect-metadata helper exposes `share311` as the per-dialect share name. `eng/run-windows-client-interop-single.ps1` parses `Smb311` with privacy required, and propagates `--enable-smb311-preview true` to `Sample.OpenCifsServer` whenever the lane is Smb311 so Windows-built-in SMB clients can negotiate the bounded SMB 3.1.1 preview against the sample host.
- Extended the Samba Docker interop harness with an SMB 3.1.1 lane: `eng/docker/samba-interop/start-samba-server.sh` now configures `server max protocol = SMB3_11`, the default `eng/run-samba-interop.ps1 -Dialects` parameter now includes `Smb311`, the dialect-metadata helper now carries `EnableSmb311Preview` and an `--option=client max protocol=SMB3_11` smbclient argument so the Samba client side opts into SMB 3.1.1 too, and the harness now passes `--enable-smb311-preview true` to `Sample.OpenCifsServer` on the SMB 3.1.1 lane. `OpenCIFS.SambaInterop.Console` now accepts `--dialect Smb311`, propagates `EnableSmb311Preview = true` into the client options, and labels the run `SMB 3.1.1`. The Interop README-status assertion now asserts the new SMB 3.1.1 preview phrasing plus the `OpenCifsClientBuilder.WithSmb311Preview()` call site. External Samba SMB 3.1.1 evidence will be recorded in `docs/interop-matrix.md` after the next `eng/run-samba-interop.ps1` run.
- Wired the bounded SMB 3.1.1 preview into `Sample.OpenCifsServer` so testers can reach it from the published bundle without code changes: new `SampleServerConfiguration.EnableSmb311Preview` setting (default `false`), new `--enable-smb311-preview <bool>` command-line override, and the configuration banner plus startup banner now report the preview state. The setting flows through to `OpenCifsServerOptions.EnableSmb311Preview` so the bounded preview is activated end-to-end on the sample host when enabled.
- Wired SMB 3.1.1 server-side NETNAME context capture: `OpenCifsServerHost.HandleNegotiate(...)` now decodes the client-supplied `SMB2_NETNAME_NEGOTIATE_CONTEXT_ID` value when an SMB 3.1.1 preview request carries one and exposes it through the new `GetReceivedClientNetname()` accessor for diagnostic inspection. Per MS-SMB2 §3.3.5.4 the NETNAME context is informational; the server SHOULD record it but is not required to reject mismatches. Shared server coverage pins capture-on-opt-in plus null-on-opt-out behavior.
- Added a shared interop loopback test pinning bounded SMB 3.1.1 preview dialect selection: when both `OpenCifsClientSession` and `OpenCifsServerHost` opt into `EnableSmb311Preview`, the client advertises four typed Preauth/Signing/Encryption/NETNAME contexts, the server negotiates SMB 3.1.1 with at least one typed response context, the client tracks the negotiated SMB 3.1.1 dialect, and both sides allocate the preauth integrity transcript hash accumulator. Coverage matrix SMB 3.1.1 negotiate-contexts row updated to reflect AES-GMAC signing plus spec-strict client-side response validation.
- Added SMB 3.1.1 client-side spec-compliance validation on the negotiate response: when the negotiated dialect is `Smb311`, the client now rejects responses with zero negotiate-context entries and rejects responses that omit the mandatory `SMB2_PREAUTH_INTEGRITY_CAPABILITIES` context. Per-context validation also asserts the preauth context selects exactly one supported hash algorithm (SHA-512) and the encryption context selects exactly one supported cipher (AES-128-CCM or AES-128-GCM). Shared client coverage pins both rejection paths.
- Wired AES-GMAC per-message signing into the bounded SMB 3.1.1 preview slice end-to-end. New `Smb2SigningNonce.BuildSmb311GmacNonce(messageId, isServerToClient)` helper in `OpenCIFS.Security` constructs the 12-byte AES-GMAC nonce per MS-SMB2 §3.1.4.1 (8 bytes MessageId LE + 3 bytes zero + 1 byte 0x80 for server→client / 0x00 for client→server). `OpenCifsClientSession` and `OpenCifsServerHost` signing and verification call sites now compute the per-message nonce when `IMessageSigner.RequiresNonce` is true. `Smb311NegotiateContextSelector.SelectSigningAlgorithm(...)` now prefers AES-GMAC over AES-CMAC and HMAC-SHA256, the SMB 3.1.1 preview client advertises `[AesGmac, AesCmac, HmacSha256]`, and the bounded both-sides-opted-in live-listener test now completes a full encrypted SMB 3.1.1 authenticated session under AES-GMAC signing.
- Strengthened the bounded SMB 3.1.1 preview both-sides-opted-in live-listener test to explicitly assert that the negotiated dialect equals `SmbDialect.Smb311` end-to-end, no longer relying on implicit signing-key compatibility to detect dialect-selection failure.
- Wired AES-128-GCM cipher selection into the bounded SMB 3.1.1 preview slice end-to-end: `Smb311NegotiateContextSelector.SelectCipher(...)` now prefers AES-128-GCM and falls back to AES-128-CCM, the client advertises `[Aes128Gcm, Aes128Ccm]`, the server selects the strongest offered cipher and emits it in the response, both sides track the negotiated cipher per session, and `Smb3MessageTransform` now switches between AES-128-CCM (11-byte nonce) and AES-128-GCM (12-byte nonce) per the negotiated cipher with the appropriate AEAD algorithm. SMB 3.1.1 key derivation now uses the negotiated cipher identifier so signing and encryption keys are derived correctly for either cipher. The bounded both-sides-opted-in live-listener test now completes a full encrypted SMB 3.1.1 session with AES-128-GCM. AES-256-GCM and AES-256-CCM cipher selection plus per-session use remain backlog.
- Wired spec-compliant typed server-side response context emission for the bounded SMB 3.1.1 preview slice: `OpenCifsServerHost.HandleNegotiate(...)` now decodes the client's typed Preauth/Encryption/Signing negotiate-context entries, applies `Smb311NegotiateContextSelector` to pick the strongest currently-supported algorithms (SHA-512 / AES-CMAC / AES-128-CCM), generates a fresh 32-byte server preauth salt, and emits typed Preauth + Encryption (when client offered encryption) + Signing (when client offered signing) response contexts on the wire whenever the negotiated dialect is `Smb311`. `Smb2CompoundPayloadHelper.GetNegotiateResponseLength(...)` now correctly handles the SMB 3.1.1 negotiate response shape that carries both a security buffer and a negotiate-context block, so compound-trim no longer rejects the new wire form. Shared server coverage pins typed Preauth + Encryption + Signing response contexts with selected SHA-512 + AES-128-CCM + AES-CMAC, and the existing both-sides-opted-in live-listener test continues to complete a full authenticated SMB 3.1.1 session with the typed response contexts now flowing on the wire.
- Activated end-to-end bounded SMB 3.1.1 dialect selection on the managed direct-TCP path under the preview opt-in: when `OpenCifsClientBuilder.WithSmb311Preview()` and `OpenCifsServerBuilder.WithSmb311Preview()` are both enabled, the server's `GetMaximumImplementedDialect()` now lifts to `SmbDialect.Smb311`, the negotiate exchange selects SMB 3.1.1 against an SMB 3.1.1 preview client, the captured preauth integrity transcript hash is passed into `SmbSessionKeyDerivation` as the SMB 3.1.1 key-derivation context on both sides, and AES-CMAC signing under the derived SMB 3.1.1 keys completes a full authenticated session with directory create / file write / file read / disconnect on the managed live-listener path. Shared server coverage pins SMB 3.1.1 selection when both sides opt in plus SMB 3.0.2 fallback when the server has not opted in. AES-GCM and AES-256-CCM cipher selection, typed server-side response context emission, NETNAME server-name verification, and external SMB 3.1.1 interop remain backlog.
- Extended SMB 3.1.1 preauth integrity transcript hashing through the session-setup exchange on both sides: `OpenCifsClientConnection.AuthenticateAsync(...)` now appends the initial session-setup request, the `MoreProcessingRequired` challenge response, and the authenticate-leg request to the preauth transcript before deriving keys; `OpenCifsServerHost.HandleCompoundRequestEntry(...)`'s `Smb2Command.SessionSetup` branch mirrors that pattern by appending each session-setup request and only the non-final `MoreProcessingRequired` response, intentionally omitting the final `Success` response so the transcript captures the same byte sequence on both sides per MS-SMB2 key-derivation semantics. The bounded both-sides-opted-in live-listener test continues to complete an authenticated session via the SMB 3.0.2 tolerance fallback while exercising the full session-setup transcript path. SMB 3.1.1 key derivation activation and end-to-end SMB 3.1.1 dialect selection remain backlog.
- Wired automatic SMB 3.1.1 preauth transcript hashing into the server-side dispatch path: `OpenCifsServerHost.HandleCompoundRequestEntry(...)` now appends the negotiate request bytes (`requestHeader.ToByteArray() + trimmedPayload`) plus the negotiate response bytes (`responseHeader.ToByteArray() + responseBody`) to the preauth integrity transcript whenever the SMB 3.1.1 preview opt-in is active and the request carries 3.1.1-shaped contexts. Test helper `StartDirectTcpServerAsync(...)` now exposes an `enableSmb311Preview` parameter, and a new shared client suite pins both-sides-opted-in behavior through a live listener: the SMB 3.1.1 preview client connects to a preview-enabled server, both sides allocate transcripts during negotiate, the existing tolerance path still selects SMB 3.0.2, and the authenticated write/read/disconnect cycle completes successfully. Transcript carry-through into session-setup and SMB 3.1.1 key derivation wiring remain backlog and continue to gate end-to-end SMB 3.1.1 dialect selection.
- Mirrored the bounded SMB 3.1.1 preauth integrity transcript hashing onto the server side: `OpenCifsServerBuilder.WithSmb311Preview()` plus `OpenCifsServerOptions.EnableSmb311Preview` and `OpenCifsServerSettings.EnableSmb311Preview` are new opt-in entry points (default off), `OpenCifsServerHost.HandleNegotiate(...)` now allocates a SHA-512 `PreauthIntegrityHashAccumulator` whenever the opt-in is active and the inbound request carries SMB 3.1.1-shaped contexts, and `AppendPreauthMessageBytes(header, body)` plus `GetCurrentPreauthIntegrityHash()` plumbing methods let the dispatch layer feed and inspect the transcript. Shared server coverage pins zero-start, request-advance, and response-advance behavior for the SMB 3.1.1 preauth transcript on a typed 3.1.1-shaped request. Transcript carry-through into session-setup and SMB 3.1.1 key derivation wiring remain backlog and continue to gate end-to-end SMB 3.1.1 dialect selection.
- Wired bounded client-side SMB 3.1.1 preauth integrity transcript hashing across the negotiate exchange: when the SMB 3.1.1 preview opt-in is enabled, `OpenCifsClientSession` now allocates a SHA-512 `PreauthIntegrityHashAccumulator` at request creation time and exposes `AppendPreauthMessageBytes(...)` plus `GetCurrentPreauthIntegrityHash()` plumbing methods, and `OpenCifsClientConnection.NegotiateAsync(...)` now appends both the negotiate request and response message bytes to the transcript. Shared client coverage pins the zero-start state, the request-advance, and the response-advance behavior. Session-setup transcript carry-through and SMB 3.1.1 key derivation remain backlog and continue to gate end-to-end SMB 3.1.1 dialect selection.
- Added the bounded client-side SMB 3.1.1 preview slice on the negotiate path: `OpenCifsClientBuilder.WithSmb311Preview()` plus `OpenCifsClientOptions.EnableSmb311Preview` are new opt-in entry points (default off) that lift the client's maximum implemented dialect to `Smb311` and emit four typed negotiate-context entries on the wire (Preauth integrity with SHA-512 + 32-byte salt, Signing with AES-CMAC + HMAC-SHA256, Encryption with AES-128-CCM, NETNAME with the configured server name) using the new `Smb2NegotiateRequest.SetNegotiateContextEntries(...)` accessor. Shared client coverage pins the default opt-out shape, the preview opt-in dialect-list inclusion plus typed-context emission, and a live-listener round trip where the existing server tolerance behavior selects SMB 3.0.2 against a preview client and an authenticated write/read/disconnect cycle completes successfully through that fallback. Server-side opt-in advertisement, transcript hashing carry-through into negotiate + session-setup, and SMB 3.1.1 key derivation wiring remain backlog and gate end-to-end SMB 3.1.1 dialect negotiation.
- Added the bounded SMB 3.1.1 negotiate-context algorithm selector: `Smb311NegotiateContextSelector` is a new pure-function static helper in `OpenCIFS.Protocol` that picks SHA-512 for preauth integrity, the strongest currently-supported signing algorithm (AES-CMAC preferred, HMAC-SHA256 fallback, AES-GMAC declined until per-message GMAC signing wiring lands), and AES-128-CCM as the only currently-supported cipher. Shared core suite pins the selection logic plus null and unsupported-offer rejection paths. The selector encapsulates the algorithm-choice logic that future SMB 3.1.1 negotiate-time wiring will need without changing any current dialect advertisement or per-message behavior.
- Closed the Milestone 3 SMB 2.0.2 exit criterion for `Sample.OpenCifsServer` mounted from Windows and Samba using the documented defaults: 2026-04-30 evidence in `docs/interop-matrix.md` covers both directions across SMB 2.0.2, SMB 2.1, and encryption-required SMB 3.0.2 with port `4450`, signing required, NTLMv2 only, anonymous and SMB1 disabled, and SMB 3.x encryption required defaults. The matched plan checkbox is now lifted from `[ ]` to `[x]`.
- Verified end-to-end interop of the SMB 3.1.1 negotiate-context codec foundation with the existing server tolerance behavior: shared server suite now pins that a typed SMB 3.1.1 negotiate-context request built through `SetNegotiateContextEntries(...)` round-trips through the wire format and through `OpenCifsServerHost.HandleNegotiate(...)`, the server selects the highest implemented dialect (SMB 3.0.2 in the bounded slice), and the typed `PreauthIntegrityCapabilities`, `EncryptionCapabilities`, and `NetnameNegotiateContext` payloads decode correctly through the existing tolerance path.
- Added the bounded Milestone 6 SMB 3.1.1 negotiate-context codec foundation plus tamper rejection: `OpenCIFS.Protocol` now carries `EncryptionCapabilities`, `NetnameNegotiateContext`, `Smb2NegotiateContextEntry`, and `Smb2NegotiateContextList` alongside the existing `PreauthIntegrityCapabilities` and `SigningCapabilities` models, with shared core round-trip plus malformed-input coverage for each model. The typed 8-byte-aligned context-list codec round-trips mixed Preauth/Encryption/Signing payloads through both `Smb2NegotiateRequest` and `Smb2NegotiateResponse` via `SetNegotiateContextEntries(...)` / `DecodeNegotiateContextEntries()`, and shared core suite now also pins inflated-entry-count rejection, zeroed hash-algorithm-count rejection, bytewise hash-algorithm tamper detection on a decoded preauth payload, and SMB 3.1.1 negotiate-request rejection when the carried negotiate-context offset is tampered below the fixed header. Coverage matrix now also splits `SMB2_COMPRESSION_CAPABILITIES`, `SMB2_TRANSPORT_CAPABILITIES`, and `SMB2_RDMA_TRANSFORM_CAPABILITIES` into individual backlog rows. Negotiation-time selection, preauth transcript hashing wiring, AES-GMAC per-message signing, AES-GCM/AES-256-CCM encryption negotiation, and SMB 3.1.1 dialect advertisement remain backlog so the dialect stays unadvertised.
- Closed Workstream B's last open negative-test helpers item: `ReplayAttemptUtilities` in `OpenCIFS.Core.Tests.Shared` now exposes deterministic replay-copy and replay-burst helpers, with shared core suite coverage that also asserts mutation isolation and null/zero-count rejection. SMB2 replay defense remains implicit through the existing message-id one-shot reuse rejection on the managed server path.
- Added a bounded public exception-taxonomy slice across the documented client and server surfaces, including `OpenCifsClientException`, `OpenCifsClientStateException`, `OpenCifsClientProtocolException`, `OpenCifsStatusException`, `OpenCifsServerException`, `OpenCifsServerConfigurationException`, and `OpenCifsServerStateException`, with shared regression coverage and updated README/package guidance before the later bounded primary-client result-envelope pass.
- Added the bounded Milestone 5 SMB 3.0 / SMB 3.0.2 opt-in slice on the managed path: `OpenCifsClientSession` and `OpenCifsServerHost` now advertise and negotiate `Smb30` / `Smb302` only when the client sets `PreferEncryption = false` and the server relaxes `RequireEncryptionForSmb3`, `OpenCifsDirectTcpServer` now carries the same advertisement through the SMB1 multi-protocol bootstrap bridge, and both client and server now derive SMB 3.x signing keys and validate/sign authenticated packets with AES-CMAC under negotiated SMB 3.0 and SMB 3.0.2. Shared client, server, and loopback coverage now verifies opt-in `Smb302` selection, clamp-to-`Smb30`, bounded authenticated session/tree lifecycle under `Smb302`, and AES-CMAC request/response validation, and the primary server builder now exposes `WithSmb3EncryptionRequired(...)` so the bounded SMB3 slice is reachable from the aligned public surface. SMB 3.x encryption and external SMB3 interop remain backlog.
- Added the bounded Milestone 8 DFS referral slice on the managed path: `OpenCIFS.Protocol` now carries `DfsReferralRequest`, `DfsReferralResponse`, `DfsReferralEntryV2`, and `DfsReferralHeaderFlags` codecs (the version-2 fixed header length was also corrected from 18 to 22 so referral string offsets land on the correct bytes), `OpenCifsClient.ResolveDfsPathAsync` / `TryResolveDfsPathAsync`, `OpenCifsClientConnection.GetDfsReferralsAsync` / `TryGetDfsReferralsAsync` / `ResolveDfsPathAsync` / `TryResolveDfsPathAsync` and a managed referral cache resolve a DFS path via `FSCTL_DFS_GET_REFERRALS` over `IPC$`, the client now advertises `SMB2_GLOBAL_CAP_DFS` consistently with that surface, `OpenCifsServerBuilder.AddDfsReferral` and `OpenCifsServerHostBuilder.AddDfsReferral` register bounded DFS referrals while the managed server now exposes `IPC$` whenever DFS referrals are configured, and `OpenCifsServerHost` answers `FSCTL_DFS_GET_REFERRALS` with the original request server name preserved in the response. Shared core suite pins request and response codec round-trip plus malformed-input rejection, shared server suite pins null rejection, zero-TTL validation, duplicate-referral rejection, and clone-time field preservation, shared client suite pins a live-listener flow with cache reuse on the second resolve call. Coverage matrix DFS row moved from backlog to implemented for the bounded scope. External Windows and Samba DFS interop, multi-target referrals, namespace-root referrals, and DFS EX requests remain backlog.
- Expanded the bounded `IPC$` / named-pipe / `srvsvc` slice so the exercised share-inspection path also stays inside OpenCIFS: `OpenCifsClient` now exposes `GetShareInfoAsync(...)` / `TryGetShareInfoAsync(...)`, `OpenCifsClientConnection` now exposes `GetRemoteShareInfoAsync(...)` / `TryGetRemoteShareInfoAsync(...)`, and `OpenCifsClientRpcException` now preserves bounded RPC service failures with service/operation/return-code detail plus normalized categories. The managed `OpenCIFS.Server` path now extends its built-in `srvsvc` endpoint from bounded `NetrShareEnum` to bounded `NetrShareGetInfo`, `OpenCIFS.TestClient` now drives that path through `shareinfo <share>`, `eng/run-test-console-smoke.ps1` now proves `shareinfo` against `OpenCIFS.TestServer`, shared core/client/interop coverage now pins bounded `srvsvc` share-info success plus malformed-input and missing-share rejection, and the Samba interop harness now verifies remote share inspection through OpenCIFS itself. Broader RPC services remain backlog.
- Expanded the bounded named-pipe slice so more of the exercised stack stays inside OpenCIFS: `OpenCifsClient` and `OpenCifsClientConnection` now expose `TransceiveNamedPipeAsync(...)` and `TryTransceiveNamedPipeAsync(...)` for generic `IPC$` / `FSCTL_PIPE_TRANSCEIVE` traffic, `OpenCifsServerNamedPipeEndpoints` now exposes a built-in `CreateUtf8EchoEndpoint()` helper plus `DefaultUtf8EchoPipeName`, and the aligned server builders now expose `AddUtf8EchoNamedPipeEndpoint()`. `OpenCIFS.TestClient` now drives bounded UTF-8 named-pipe traffic with `pipe <name> <text>`, `OpenCIFS.TestServer` now registers the bounded echo endpoint by default and exposes it through a `pipes` command, `eng/run-test-console-smoke.ps1` now verifies share browsing plus named-pipe transceive end to end, shared client coverage now pins positive and negative pipe transceive behavior, shared server coverage now pins generic endpoint registration plus duplicate rejection, and shared loopback interop coverage now pins a header-wrapped `IPC$` / `FSCTL_PIPE_TRANSCEIVE` echo flow. Broader named-pipe semantics and additional RPC services remain backlog.
- Added the bounded end-to-end named-pipe share-browsing slice: `OpenCIFS.Client` now implements `OpenCifsClient.EnumerateSharesAsync(...)`, `TryEnumerateSharesAsync(...)`, `OpenCifsClientConnection.EnumerateRemoteSharesAsync(...)`, and `TryEnumerateRemoteSharesAsync(...)` through `IPC$`, `srvsvc`, `FSCTL_PIPE_TRANSCEIVE`, and a bounded DCE/RPC `NetrShareEnum` level-1 path, and the managed `OpenCIFS.Server` path now hosts bounded `IPC$` / named-pipe endpoints through `OpenCifsServerBuilder.AddNamedPipeEndpoint(...)` plus the built-in `AddSrvsvcShareEnumerationEndpoint()` share-enumeration endpoint. Shared core coverage now pins the SRVSVC request and response codecs plus malformed-input rejection, shared client coverage pins the public API shape plus the negative no-`IPC$` failure path, shared server coverage pins `IPC$` share exposure when the bounded `srvsvc` endpoint is registered, `eng/run-test-console-smoke.ps1` now proves `OpenCIFS.TestClient shares` against `OpenCIFS.TestServer` end to end through OpenCIFS itself, and the Samba interop harness still proves real remote share browsing across SMB 2.0.2, SMB 2.1, and SMB 3.0.2 with `BrowsedShareNames` including the configured share and `IPC$`. Broader pipe semantics, DFS, and other RPC services remain backlog.
- Added menu-driven `OpenCIFS.TestClient` and `OpenCIFS.TestServer` manual tester consoles plus `eng/run-test-console-smoke.ps1`. `OpenCIFS.TestClient` now provides a bounded manual client exercise surface for endpoint or credential configuration, remote share browsing through OpenCIFS, share open, directory traversal and enumeration, metadata review, file read or write or rename or delete, and local upload or download on top of the current public OpenCIFS client APIs. `OpenCIFS.TestServer` now provides a bounded manual server exercise surface for bind or credential or dialect configuration, temporary backing-store management, bounded built-in `srvsvc` share browsing, request logging, and managed listener start or stop on top of the current public OpenCIFS server APIs. The smoke harness drives both consoles through redirected stdin, verifies a real end-to-end flow, and records evidence in `artifacts/test-console-smoke/test-console-smoke.json`.
- Added the bounded Milestone 5 SMB 3.0.2 non-persistent durable-handle v2 reconnect slice: the managed client and server now emit and validate `DH2Q`, `DH2C`, and `DH2R` create contexts on the SMB3 direct-TCP path, preserve durable create GUID plus bounded timeout state across detach and reconnect, keep shared persistent file identifiers plus fresh volatile file identifiers on reconnect, and preserve detached byte-range lock ownership across reconnect on the managed path. Shared client, server, and encrypted loopback coverage now pins positive and negative durable-handle v2 request or reconnect or response handling, bounded persistent-handle rejection, and encrypted SMB 3.0.2 durable-handle v2 reconnect recovery, while broader continuous-availability, persistent clustered-handle, lease-v2, multichannel, and external durable-handle v2 interop coverage remain backlog.
- Added the bounded Milestone 5 secure-negotiate validation slice: `OpenCifsClientSession` now captures the original negotiated client and server state needed to build and verify `FSCTL_VALIDATE_NEGOTIATE_INFO`, `OpenCifsClientConnection` now runs bounded SMB3 secure-negotiate validation automatically on the first tree-connect path and exposes explicit advanced/raw revalidation helpers, and `OpenCifsServerHost` now rejects validate-negotiate requests whose offered dialect list does not exactly match the original negotiated client dialect offer. Shared client, server, and loopback coverage now pins positive and negative secure-negotiate request/response matching, live encrypted SMB 3.0.2 direct-TCP auto-validation, and mismatch rejection, and the package/readme/coverage claims now reflect the bounded managed SMB 3.0 / SMB 3.0.2 secure-negotiate slice instead of treating it as backlog.
- Added a bounded Milestone 5 SMB 3.0.2 encrypted interop slice: the managed client and server now complete AES-128-CCM encrypted SMB 3.0.2 session, tree, file-I/O, realistic related-compound, and durable batch-reconnect flows on the managed path; `Smb2NegotiateRequest` now tolerates the SMB 3.1.1-style negotiate-request shapes current Windows and Samba clients send; `Smb2CreateRequestValidator`, `Smb2CreateContextCodec`, and `OpenCifsServerHost` now accept bounded SMB3 durable or lease create hints on fresh opens without over-claiming durable-handle v2 support; and the bounded Python real-client, Samba client/server, native Windows mapped-drive, and nightly interop harnesses now all rerun SMB 2.0.2, SMB 2.1, and encryption-required SMB 3.0.2 with current evidence. Shared loopback coverage now also pins encrypted file-I/O, encrypted realistic compounding, and encrypted durable reconnect.
- Added a bounded deeper current-dialect interop automation slice: `eng/run-nightly-interop.ps1` now composes repeated dual-dialect Python real-client, Samba, and native Windows interop reruns with a larger bounded payload plus a stronger SMB 2.1 soak for durable reconnect, exclusive oplock-break, lease-break, and large-I/O churn, and records the composed artifact in `artifacts/nightly-interop/nightly-interop.json`. The external interop scripts now accept an explicit large-payload override, the Samba client path now really exercises large-file `put`/`get`, and the native Windows mapped-drive path now carries the same larger payload through its hash-checked large-file workflow. Encryption-specific nightly coverage remains backlog until the SMB 3.x encryption milestones land.
- Added the shared test-utility infrastructure slice: `OpenCIFS.Core.Tests.Shared` now exposes reusable test-environment defaults, deterministic temp-path cleanup helpers, deterministic test clocks and cross-process hash seeds, a repository-backed golden-vector loader, and deterministic packet-trace capture helpers. Core security tests now load representative crypto and NTLM known-answer vectors from `docs/protocol-notes/golden-vectors/security-vectors.json`, the bootstrap suite now positively and negatively validates the new utility layer, the client/server/interop suite helper paths now consume the shared defaults and cleanup behavior, and the malformed direct-TCP mutation suite now records representative baseline/mutation/recovery traces while using a truly stable deterministic seed instead of process-randomized string hashes.
- Added a bounded external-tester sample-host slice: `OpenCIFS.Server` now exposes immutable `OpenCifsServerSettings` plus a clearer `OpenCifsServerBuilder -> OpenCifsServer -> OpenCifsServerApplication` runtime path, `Sample.OpenCifsServer` now uses that configured-server surface, `eng/publish-sample-server.ps1` now emits a tester-facing published bundle with generated launcher scripts and a bundle manifest, and `eng/run-published-sample-smoke.ps1` now validates the published executable host plus helper-launcher path end to end under SMB 2.0.2 and SMB 2.1. The shared server suite, README smoke, and broader integration gate were updated to exercise the new runtime shape.
- Expanded the operational soak slice: `eng/run-soak-smoke.ps1` now runs a timed SMB 2.1 soak instead of a fixed small-loop smoke, drives the primary `OpenCifsClient` -> `OpenCifsShareSession` surface for repeated connection churn plus large-file round trips, and adds advanced durable-reconnect, exclusive-oplock-break, and lease-break churn on the same managed listener. `eng/validate-release-artifacts.ps1` now validates the minimum soak duration plus durable/oplock/lease counters before accepting the artifact, and the `Release` gate was rerun successfully after the expansion.
- Added a package-facing claim-hardening slice: `eng/OpenCIFS.Build` now exposes `validate-package-claims`, `src/Directory.Build.targets` now runs it during normal solution builds, `eng/validate-release-artifacts.ps1` now reruns it as part of the `Release` gate, and `eng/run-package-smoke.ps1` now validates exact bounded nuspec descriptions plus required package-readme scope phrases before restoring the downstream consumer. This keeps `OpenCIFS.Protocol`, `OpenCIFS.Client`, and `OpenCIFS.Server` publishable without letting `OpenCIFS.Transport` or `OpenCIFS.Security` drift out of their documented advanced-dependency posture. The full `Release` gate, including package smoke, README smoke, Python real-client, Samba, native Windows, and soak validation, was rerun successfully after the change.
- Added a bounded parser-mutation and malformed-input hardening slice: `OpenCIFS.Core.Tests.Shared` now exposes deterministic mutation utilities and runs representative mutation corpuses across direct-TCP frame headers, SMB negotiate, create, change-notify, IOCTL, lease-break, query-info, metadata, and compounding readers, while `OpenCIFS.Server.Tests.Shared` now drives malformed direct-TCP request bursts through a live listener and verifies bounded protocol-exception surfacing plus successful post-burst negotiate recovery. Validation surfaced three real robustness gaps that are now fixed: zero-length direct-TCP mutation payloads are sent as raw malformed headers instead of being blocked by the test helper, `DirectTcpFrameHeader.ReadFrom(...)` now normalizes invalid zero or oversized lengths into `ProtocolEncodingException`, and `OpenCifsDirectTcpServer` now contains top-level malformed-request parser faults during dispatch and connection disposal instead of letting them escape as listener-tearing `OverflowException` or post-dispose read-loop faults. The full Debug integration gate, including package smoke, README smoke, Python real-client, Samba, and native Windows interop, was rerun successfully after the change.
- Added a GA-hardening and publish-readiness slice: `eng/OpenCIFS.Build` now exposes `validate-source-audit`, `src/Directory.Build.targets` now runs that validator during normal solution builds, and `eng/validate-release-artifacts.ps1` now enforces current source-audit success plus dialect-aware interop artifact validation before `Release` claims are accepted. The audit currently fails product code on `NotImplementedException` and fails product and release-facing surfaces on TODO or placeholder or stub language. Validation also surfaced a real native Windows harness problem: back-to-back custom-port `New-SmbMapping` runs could fail the second dialect with `The network path was not found`. `eng/run-windows-client-interop-single.ps1` and `eng/run-windows-client-interop.ps1` now use isolated per-dialect share names, explicit child-script exit behavior, and longer mapping retries so the dual-dialect Windows matrix is stable again. The full `Release` gate was rerun successfully after the change.
- Added the public-surface hardening slice: `OpenCifsClientConnection` now exposes bounded advanced/raw `Try...Async` result-envelope companions across connect or authenticate or tree or open or read or write or query or set or notify or realistic compound or close or disconnect flows, `OpenCifsServerApplication` now exposes bounded `TryRunAsync` or `TryStartAsync` or `TryStopAsync` lifecycle companions returning `OpenCifsServerResult`, and the managed server application path now preserves typed `OpenCifsServerStateException` failures for bind conflicts, double-start, and disposed-lifecycle misuse instead of leaking raw listener exceptions. Shared client and server suites now pin the new API shape plus positive and negative result-envelope behavior, the advanced/raw client tests now assert preserved local argument-validation throws, the Windows dual-dialect interop wrapper now validates per-dialect workflow artifacts and retries transient mapped-drive teardown races instead of false-greening on a failed second dialect run, and the documented README/package-readme/compatibility/coverage-plan surfaces were updated to claim only this verified bounded expansion.
- Added the bounded primary-client non-throwing result-envelope slice: `OpenCifsClient`, `OpenCifsShareSession`, and grouped `Files` or `Directories` or `Metadata` or `Locks` operations now expose `Try...Async` companions returning `OpenCifsClientResult` / `OpenCifsClientResult<T>`, preserving typed client failures, SMB2 command, NTSTATUS, normalized error category, and error-data detail for expected negative paths while leaving local argument-validation and cancellation errors throwing. This slice also tightened the public disposed-state semantics on the documented client surfaces, added a dedicated non-throwing sample in `docs/samples/OpenCifsClientResultEnvelopeHappyPath.cs`, expanded shared primary-surface API-shape and success/failure coverage, and updated the README, package readme, compatibility checklist, and coverage matrix to claim only the verified bounded result-envelope surface while advanced/raw and server envelopes remain backlog.
- Automated the bounded external interop stack across every merged dialect milestone: `eng/run-real-client-interop.ps1`, `eng/run-samba-interop.ps1`, and `eng/run-windows-client-interop.ps1` now rerun their verified smoke flows under both SMB 2.0.2 and SMB 2.1 and emit combined per-dialect evidence artifacts. `OpenCIFS.SambaInterop.Console` and `eng/real-client-smoke.py` are now dialect-aware, the Windows mapped-drive path now uses a single-dialect worker plus a matrix wrapper to keep back-to-back native-client runs stable, `eng/run-integration-gates.ps1` now enforces the dual-dialect external stack, and `eng/validate-release-artifacts.ps1` now rejects external interop evidence that is missing either claimed dialect run. The Debug build and all three external interop harnesses were rerun after the change.
- Added the bounded SMB 2.1 durable-reconnect slice: the server now grants durability for lease-backed opens that request handle caching, preserves lease state across detach and reconnect, rejects missing or mismatched lease reconnect state, preserves same-client-guid validation, and downgrades the reconnected lease state to `SMB2_LEASE_NONE` when a competing open exists while the durable lease-backed open is detached. `OpenCifsClientConnection` now preserves its SMB client GUID across transport reconnects so durable lease reconnect works on the managed path, and shared server, client, and loopback coverage now exercises positive and negative SMB 2.1 lease-backed durable reconnect flows alongside the full Debug build, Touchstone matrix, and broader integration gate.
- Added the next major SMB 2.1 compound-flow slice: `OpenCifsClientConnection` now exposes bounded realistic `CompoundCreateQueryInfoCloseAsync`, `CompoundOpenReadCloseAsync`, and `CompoundCreateWriteFlushCloseAsync` helpers, while `OpenCifsShareSession.Files.ReadAllBytesAsync`, `WriteAllBytesAsync`, and `Metadata.GetAttributesAsync` now opportunistically route small direct-TCP reads, writes, and `FILE_ALL_INFORMATION` queries through those related compound chains. Shared client and loopback coverage now verifies positive and negative realistic `create -> query -> close`, `open -> read -> close`, and `create -> write -> flush -> close` flows, and validation surfaced three real bugs that are now fixed: related request headers on the client were stripping `Signed`, related response headers on the server were stripping `Signed`, and read-only `FILE_OPEN` of a missing file was incorrectly returning `STATUS_ACCESS_DENIED` before `STATUS_OBJECT_NAME_NOT_FOUND`. The full Debug build, Touchstone matrix, and later integration gate were rerun after the change.
- Added the next compatibility-cleanup slice: `OpenCifsStatusException` now exposes normalized `OpenCifsErrorCategory` alongside the failing SMB2 command, NTSTATUS, and decoded error data; shared client coverage now verifies representative category mapping plus real flow coverage for access-denied, conflict, and protocol-error cases; the first server README example now truthfully uses `BuildApplication()` with `RunAsync(...)`; the docs now explicitly separate the primary high-level surface from the advanced/raw SMB surface; and a dedicated copy-pasteable high-level client sample now lives in `docs/samples/OpenCifsClientHappyPath.cs`.
- Added bounded server-side share introspection across `OpenCifsServerBuilder`, `OpenCifsServerHostBuilder`, `OpenCifsServerHost`, and `OpenCifsServerApplication` through immutable `OpenCifsServerShareInfo` snapshots, and updated the README, server package readme, README smoke, package smoke, and shared server suites to validate explicit-share and implicit-options-share enumeration without claiming remote SMB share browsing over `IPC$` or `srvsvc`. This slice also completed the partially landed application-constructor wiring so the full solution builds cleanly again.
- Completed the first compatibility-pass public-surface alignment: added immutable `OpenCifsClientSettings`, `OpenCifsClientBuilder`, `OpenCifsClient`, `OpenCifsShareSession`, grouped `Files` or `Directories` or `Metadata` or `Locks` APIs, `OpenCifsServerBuilder`, and a share-builder path for local filesystem registration; moved the sample host, package readmes, package smoke, README smoke, and repository README examples onto the aligned builder -> client -> share-session and builder -> application flows; and added shared API-shape plus positive and negative path-first regression coverage for the new primary surface. While validating that surface, a real signed-logoff regression surfaced on the primary client disconnect path, and the server now retains the session signing key just long enough to finalize a signed logoff response after cleanup without leaving the session reusable. The full build, targeted client xUnit suite, `eng/test.ps1`, package smoke, README smoke, and broader integration gate were rerun after the change.
- Added the bounded release-soak slice: `eng/run-soak-smoke.ps1` now compiles a temporary project-reference consumer, starts an in-process SMB 2.1 `OpenCIFS.Server`, and verifies parallel facade connection churn, repeated `200000`-byte large-file round trips, non-empty-directory delete rejection, detached durable read and lock conflict rejection, durable reconnect success, and post-reconnect competing lock recovery. `eng/run-release-gates.ps1` now runs that soak before release-artifact validation, and `eng/validate-release-artifacts.ps1` now requires and validates the resulting `artifacts/soak-smoke/soak-smoke.json` evidence. The full `Release` gate was rerun successfully after the change.
- Added the release-readiness validation slice: `eng/OpenCIFS.Build` now validates `docs/interop-matrix.md` structure alongside the existing coverage-matrix and package-graph checks, `eng/validate-release-artifacts.ps1` now rejects stale concrete verification dates and missing pass-row evidence across the coverage and interop release artifacts, and `eng/run-release-gates.ps1` now reruns the full `Release` integration stack in one command before recording a summary in `artifacts/release-gates/release-gates.json`. This slice also hardened the client-side durable-disconnect test helper so Release-matrix durable-lock reconnect coverage waits for server-side disconnect cleanup before issuing competing operations. The full Release gate, including Touchstone, framework runners, package smoke, README smoke, Python real-client, Samba, native Windows, and release-artifact validation, was rerun successfully after the change.
- Added the integration-readiness hardening slice: `eng/run-readme-smoke.ps1` now compiles a temporary downstream consumer from the documented client and server examples and verifies authenticated echo, directory create, file write or read, metadata query, enumeration, rename, cleanup, callback invocation, and non-empty-directory delete rejection; `eng/test.ps1` now includes both package-smoke and README-smoke coverage; `eng/run-integration-gates.ps1` now composes the clean build, Touchstone console suites, framework runner matrix, package smoke, README smoke, Python real-client, Samba, and native Windows interop smoke into one local gate; and the shared direct-TCP test helpers now reserve loopback ports through a named cross-process semaphore so the full multi-target matrix remains stable under parallel load. The combined integration gate was rerun successfully after the change.
- Hardened the feed-facing package slice: packable product and dependency libraries now emit package-specific readmes and richer package metadata, the package smoke harness now validates emitted nuspec metadata plus readme and XML-doc payloads before restoring the downstream consumer, and the README or roadmap documentation now reflects that verified package-consumer integration scope instead of the earlier generic repository readme packaging.
- Added the package-consumer integration slice: `OpenCIFS.Security` and `OpenCIFS.Transport` are now packable so the `OpenCIFS.Client` and `OpenCIFS.Server` package graph restores cleanly, `eng/OpenCIFS.Build` now validates that every packable project only references other packable projects, and `eng/run-package-smoke.ps1` now packs the product libraries into a local feed and verifies a generated downstream consumer can restore and run authenticated echo, directory create, file write or read, metadata query, enumeration, rename, cleanup, bad-credential rejection, and non-empty-directory delete rejection from packaged artifacts. The full build, Touchstone, and `eng/test.ps1` entrypoint were rerun after the change.
- Added the bounded SMB 2.1 large-I/O slice: `OpenCifsServerHost` now advertises `SMB2_GLOBAL_CAP_LARGE_MTU`, tracks negotiated max read or write sizes, accepts compatible overcharged multi-credit requests, and validates bounded large-I/O `CreditCharge` shapes; `OpenCifsClientSession`, `OpenCifsClientConnection`, and `OpenCifsClientFacade` now track negotiated large-I/O limits, grow the credit window through authenticated `ECHO`, build bounded multi-credit read or write requests, and chunk facade transfers when the peer credit window is smaller than the total payload. Shared client, server, and loopback coverage now exercises positive and negative large-I/O flows, and the direct-TCP client now accepts unsigned interim async `STATUS_PENDING` responses while still rejecting unsigned final async responses, matching the bounded Samba behavior surfaced during validation. The full build, Touchstone, `dotnet test`, Python real-client, Samba, and native Windows client smoke paths were rerun after the change, with verified `200000`-byte large-transfer evidence recorded in the real-client, Samba, and Windows interop artifacts.
- Added the bounded SMB 2.1 lease slice: `OpenCifsServerHost`, `OpenCifsClientSession`, and `OpenCifsClientConnection` now handle SMB 2.1 `RqLs` create contexts, read/write/handle lease grant tracking, signed lease-break notifications and acknowledgments, managed direct-TCP lease wait-and-ack handling, and rejection of same-lease-key path mismatches, invalid acknowledgments, and unexpected or unsigned notifications. Shared core, server, client, direct-TCP, and loopback coverage now exercises both positive and negative lease flows, and the full build, Touchstone, `dotnet test`, Python real-client, Samba, and native Windows client smoke paths were rerun after the change.
- Added the bounded SMB 2.1 lockstep slice: the managed client and server now negotiate and run the existing session, tree, file-I/O, metadata, notification, locking, oplock, and bounded durable-reconnect surface under SMB 2.1; the SMB1 multi-protocol bootstrap bridge now offers both `Smb2002` and `Smb21` and supplies the required non-empty SMB 2.1 client GUID; the Python `smbprotocol` harness now pins the correct SMB 2.1 dialect constant; and the full build, Touchstone, `dotnet test`, Python real-client, Samba, and native Windows client smoke paths were rerun under the verified SMB 2.1 scope.
- Added the bounded SMB 2.0.2 durable-lock carryover slice: detached durable opens now preserve byte-range lock state across abrupt transport disconnect, reconnect restores lock ownership to the re-established open, competing reads and lock requests stay rejected while the durable open is detached or reconnected, and shared server or client or loopback coverage now exercises positive unlock recovery plus negative detached and post-reconnect conflict paths. The full build, Touchstone, `dotnet test`, Python real-client, Samba, and native Windows client smoke paths were rerun after the change.
- Added explicit SMB2 status propagation on the managed client surface through `OpenCifsStatusException`, so low-level and high-level `OpenCIFS.Client` operations now preserve the failing SMB2 command and NTSTATUS instead of collapsing server-declared failures to generic operation errors. Shared client coverage now asserts exact statuses for session-setup failure, tree-connect failure, metadata failure, share-access conflict, lock conflict, and non-empty-directory delete rejection, and the Docker-backed Samba smoke now asserts the exact status-bearing client failures on its expected rejection paths. The full build, Touchstone, `dotnet test`, Samba, Python real-client, and native Windows client smoke paths were rerun after the change.
- Added the bounded SMB 2.0.2 durable-reconnect slice: SMB2 durable create and reconnect context codecs, shared persistent and volatile file IDs, detached durable-open tracking across abrupt transport disconnect, low-level client durable-open request and reconnect flows, and positive or negative core or server or client or loopback coverage for reconnect success, reconnect rejection, and nondurable-handle rejection. This slice also fixed native Windows compatibility by tolerating padded zero-length create contexts, adding `FILE_INTERNAL_INFORMATION` query support, preserving attribute-only read-only clearing across share-none without granting file-read data access, and accepting the bounded cancel-header shape seen during interop validation. The full build, Touchstone, `dotnet test`, Python real-client, Samba, and native Windows interop harnesses were rerun after the change.
- Expanded the bounded SMB 2.0.2 related-compounding slice: `OpenCifsServerHost` now dispatches synchronous related `SET_INFO`, `QUERY_INFO`, `LOCK`, and bounded open-scoped `IOCTL` entries with carried `SessionId`, generated `TreeId`, and generated `FileId` context, while shared server and loopback suites now cover positive related `tree connect -> create -> set info -> query info -> lock -> ioctl -> close -> tree disconnect` flows plus propagated create-failure rejection across later metadata, locking, IOCTL, and close legs. The full build, Touchstone, and `dotnet test` runner matrix were rerun after the change.
- Added the bounded SMB 2.0.2 oplock-break slice: `OpenCifsClientSession`, `OpenCifsClientConnection`, and `OpenCifsServerHost` now handle signed exclusive `OPLOCK_BREAK` notifications and acknowledgments, the direct-TCP server shared-state path now correctly triggers breaks across connection-scoped hosts instead of accidentally suppressing them on colliding volatile file IDs, and shared core or server or client or loopback suites now cover positive and negative oplock codec, state, direct-TCP, and loopback flows. The full build, Touchstone, `dotnet test`, Python real-client, Samba, and native Windows interop harnesses were rerun after the change.
- Added the bounded SMB 2.0.2 authenticated-signing slice: `OpenCifsClientSession`, `OpenCifsClientConnection`, `OpenCifsServerHost`, and `OpenCifsDirectTcpServer` now sign or verify authenticated SMB2 packets on the managed path, shared client or server or loopback suites now cover signed echo and cancel flows plus missing or tampered-signature rejection, session-setup response validation is explicitly bounded to stay unsigned in this slice, and the Python real-client, Samba, and native Windows interop harnesses were rerun with signing required. This slice also hardened the native Windows harness to use `\\localhost\share` so mapped-drive smoke stays isolated from pre-existing local `127.0.0.1` admin-share sessions.
- Closed the Milestone 3 SMB 2.0.2 negative-state testing gap for the current slice: shared client, server, and loopback coverage now explicitly verifies invalid-credit exhaustion, unsupported related-compound chain rejection, bad message-id rejection, stale-handle behavior, and invalid session/tree/open identifier rejection across bounded file-I/O and metadata paths.
- Expanded the bounded async notification and native Windows interoperability slices: shared server, direct-TCP client, and loopback coverage now verifies rename and delete `CHANGE_NOTIFY` completion plus non-recursive nested-create suppression and cancellation, while `eng/run-windows-client-interop.ps1` now validates mapped-drive `FileSystemWatcher` nested-create and rename delivery along with native share-access and byte-range lock-conflict rejection against `Sample.OpenCifsServer`.
- Added a bounded multi-client direct-TCP slice: `OpenCifsDirectTcpServer` now shares server-wide state across connection-scoped hosts and flushes async responses to idle clients, enabling prompt cross-connection `CHANGE_NOTIFY` completion plus shared share-access and byte-range lock-conflict enforcement. Shared client and loopback coverage now verifies low-level and facade notify waits, notify cancellation, read-share success, conflicting share-access rejection, and cross-session lock-conflict rejection over live direct-TCP listeners.
- Expanded the bounded Samba interoperability slice in both directions: `OpenCIFS.Client` to Samba now verifies nested-directory create, bounded last-write mutation, EOF truncation, directory rename, non-empty-directory delete rejection, deleted-path reopen rejection, and cleanup in addition to the earlier read/write/query flow, while the Samba client to `Sample.OpenCifsServer` path now verifies nested-directory create, directory listing, non-empty-directory delete rejection, and cleanup. This slice also fixed `OpenCifsClientConnection` so non-success `SMB2 SET_INFO` replies can carry real error bodies without being misparsed as success payloads, widened the shared direct-TCP client coverage for low-level rename and delete flows plus non-empty-directory delete rejection, and hardened the Samba harness so expected-failure checks capture and assert the real `smbclient` output.
- Expanded the bounded real-client and native Windows interoperability coverage for `Sample.OpenCifsServer`: the Python `smbprotocol` smoke now verifies read-only delete rejection, non-empty directory delete rejection, nested-directory cleanup, and dynamic loopback port selection, while the native Windows mapped-drive smoke now verifies hidden and read-only attribute mutation, read-only clearing, bounded EOF truncation, non-empty directory delete rejection, directory rename, and full cleanup. This slice also fixed server access handling so attribute-only metadata opens on read-only files can clear the read-only bit without granting file-read data access, and added matching positive and negative server and loopback coverage.
- Added a bounded native Windows interoperability slice with `eng/run-windows-client-interop.ps1`, dynamic loopback direct-TCP port selection, native `New-SmbMapping` drive-mount evidence, Windows 11 environment capture, server fixes for plain opens of existing directories and `FILE_BOTH_DIR_INFORMATION` enumeration, and a standard SPNEGO-wrapped NTLM session-setup completion fix that restores Samba `smbclient` compatibility on the final success leg.
- Added a bounded Samba interoperability slice with a reproducible Docker-backed Samba 4.17.12 client and server lab, exact version evidence in `docs/interop-matrix.md`, verified `OpenCIFS.Client` to Samba and Samba client to `Sample.OpenCifsServer` smoke runs, direct shared coverage for `FILE_ALL_INFORMATION` and `FILE_FS_SIZE_INFORMATION`, and the compatibility fixes required for `FSCTL_VALIDATE_NEGOTIATE_INFO`, bounded filesystem-size query handling, interim async `STATUS_PENDING` completion from Samba, and cancellation-tolerant direct-TCP transport shutdown.
- Added a broader client-surface pass: a non-preview managed direct-TCP `OpenCifsClientConnection` surface for connect, authenticate, tree, open, read, write, query, set, enumerate, and close flows; stable `OpenCifsClientFacade` promotion out of preview; tracked tree and open handle models; shared live-listener coverage for low-level and high-level client operations; and preserved real-client SMB 2.0.2 smoke validation for `Sample.OpenCifsServer`.
- Added the broader server-surface pass: generic share-backend contracts with explicit file, directory, metadata, locking, notification, and named-stream capability declarations; a concrete local filesystem share provider; a managed direct-TCP application wrapper with explicit start and stop lifecycle control; shared managed-lifecycle coverage; and preserved real-client SMB 2.0.2 smoke validation through the updated sample host.
- Added a bounded server callback-control slice with typed authenticated-session, tree-connect, create, query-directory, set-info, and IOCTL hooks on `OpenCifsServerHost`, plus shared-suite coverage and preserved real-client SMB 2.0.2 smoke validation.
- Expanded `Sample.OpenCifsServer` into a tester-facing executable surface with runtime configuration overrides, deterministic configuration and startup guidance, secure-default coverage, and CLI-driven real-client smoke evidence for the effective sample-host setup.
- Extended the preview `OpenCifsClientFacade` slice with high-level metadata query, bounded basic-info and file-length mutation, rename, and file or empty-directory delete operations over direct TCP, plus live-listener coverage and explicit preview marking for the returned metadata type.
- Expanded the bounded real-client SMB 2.0.2 smoke path to verify metadata query, bounded basic-info and end-of-file mutation, file rename and delete, and directory rename and delete against `Sample.OpenCifsServer` using Python `smbprotocol` 1.16.1.
- Added a bounded preview `OpenCifsClientFacade` slice with live direct-TCP connect, authenticate, echo, directory-create, file write, file read, and directory-enumeration coverage against `OpenCifsDirectTcpServer`, plus an explicit public preview marker on the incomplete ergonomic facade surface.
- Added a bounded server-surface slice with `OpenCifsServerHostBuilder`, explicit local filesystem share registration, duplicate-share rejection, builder-backed `Sample.OpenCifsServer` host creation, and server plus loopback coverage for multi-share routing and suppression of the implicit legacy options share when explicit shares are registered.
- Moved the solution file, shared `Directory.*` build files, and all test projects under `src/`, and updated repository scripts, CI, documentation, and test path helpers to build and run from the new layout.
- Added bounded real-client SMB 2.0.2 smoke validation for `Sample.OpenCifsServer` through `eng/run-real-client-interop.ps1` using Python `smbprotocol` 1.16.1, and fixed the NTLMv2 zero-LM compatibility plus compounded `QUERY_DIRECTORY` trimming and dispatch needed for external-client interoperability.
- Bootstrapped the OpenCIFS repository structure, solution, project graph, package management, build validation, and Touchstone test runner matrix.
- Added the Milestone 1 protocol foundation: binary codecs, shared frame and header models, FSCC catalogs, lifecycle state objects, validation layers, preauth transcript hashing, `System.IO.Pipelines` transport scaffolding, and expanded core Touchstone coverage.
- Added the Milestone 2 security foundation: local MD4 and HMAC-MD5, NTLMv2 response and MIC flows, SPNEGO token handling, SMB signing algorithms, HKDF and SMB session-key derivation, AES-CCM/AES-GCM wrappers, and expanded security vector and tamper coverage.
- Added the first Milestone 3 vertical slice: SMB 2.0.2 negotiate request and response codecs, dialect-selection helpers, client and server negotiation surfaces, and verified loopback negotiation coverage.
- Added the second Milestone 3 vertical slice: SMB 2.0.2 session-setup, logoff, tree-connect, and tree-disconnect codecs; in-memory client/server NTLMv2 session flow; and verified core, client, server, and loopback coverage for success and negative paths.
- Added the third Milestone 3 vertical slice: SMB 2.0.2 create, read, write, flush, and close codecs; minimal client/server open tracking backed by the local share path; and verified core, client, server, and loopback coverage for steady-state I/O, EOF, stale handles, collisions, and path rejection.
- Added the fourth Milestone 3 vertical slice: SMB 2.0.2 credit accounting and message-id enforcement with reserved `CreditCharge=0` handling, client and server sequence windows, configurable server credit limits, and verified core, client, server, and loopback coverage for stable credit reuse plus invalid flag, message-id, and malformed-header rejection.
- Added the fifth Milestone 3 vertical slice: SMB 2.0.2 unrelated compound packet framing and dispatch for the implemented negotiate, session, tree, and file-I/O commands, with zero-padding trimming, compounded header application, and verified core, client, server, and loopback coverage while `SMB2_FLAGS_RELATED_OPERATIONS` chains remain backlog.
- Added the sixth Milestone 3 vertical slice: SMB 2.0.2 share-access enforcement plus `FILE_DELETE_ON_CLOSE` and delete-pending lifecycle handling for the existing create/close path, with verified core, client, server, and loopback coverage while rename, allocation, and timestamp semantics remain backlog.
- Added the seventh Milestone 3 vertical slice: SMB 2.0.2 file-handle `QUERY_INFO` and `SET_INFO` handling for basic, standard, name, and network-open metadata plus bounded last-write and attribute updates, allocation size, end-of-file mutation, rename, and disposition behavior, with verified core, client, server, and loopback coverage while `QUERY_DIRECTORY` remains backlog.
- Added the eighth Milestone 3 vertical slice: SMB 2.0.2 `QUERY_DIRECTORY` handling for existing-directory opens, `FILE_DIRECTORY_INFORMATION`, `FILE_FULL_DIR_INFORMATION`, search-pattern filtering, restart and resume enumeration state, and bounded exhaustion/error behavior, with verified core, client, server, and loopback coverage while broader metadata backlog remains unchecked.
- Added the ninth Milestone 3 vertical slice: SMB 2.0.2 byte-range `LOCK` handling with shared and exclusive range ownership tied to opens, exact-range unlock semantics, competing lock/read/write conflict rejection, and close cleanup, with verified core, client, server, and loopback coverage while `CANCEL` and oplock or lease break flows remain backlog.
- Added the tenth Milestone 3 vertical slice: SMB 2.0.2 authenticated-session `ECHO` handling with fixed-body request and response codecs, client keepalive flow, compound payload trimming, and verified core, client, server, and loopback coverage while unsigned `SessionId=0` echo semantics remain backlog.
- Added the eleventh Milestone 3 vertical slice: SMB 2.0.2 bounded related compounding for synchronous tree and file-I/O chains, with carried `SessionId`, generated `TreeId`, generated `FileId`, related response flags, and mixed-style rejection verified across core, client, server, and loopback suites while broader related-chain and asynchronous compounding behavior remain backlog.
- Added the twelfth Milestone 3 vertical slice: SMB 2.0.2 bounded `IOCTL` handling with generic request and response codecs, explicit unsupported-FSCTL behavior for open-scoped and wildcard-file-id requests, stale-handle and malformed-shape rejection, unrelated compound dispatch, and verified core, client, server, and loopback coverage while specific FSCTL implementations and async command behavior remain backlog.
- Added the thirteenth Milestone 3 vertical slice: SMB 2.0.2 bounded `FSCTL_SRV_ENUMERATE_SNAPSHOTS` handling with open-scoped request and response codecs, empty `SRV_SNAPSHOT_ARRAY` success responses, invalid-shape rejection, and verified core, client, server, and loopback coverage while broader FSCTL implementations and async command behavior remain backlog.
- Added the fourteenth Milestone 3 vertical slice: SMB 2.0.2 bounded directory-create handling for `FILE_DIRECTORY_FILE` `FILE_CREATE`, `FILE_OPEN`, and `FILE_OPEN_IF`, with backing-directory materialization, clean collision and `STATUS_NOT_A_DIRECTORY` behavior, and verified core, client, server, and loopback coverage while broader create-option semantics remain backlog.
- Added the fifteenth Milestone 3 vertical slice: SMB 2.0.2 bounded empty-directory delete-pending handling for `FILE_DIRECTORY_FILE` `FILE_DELETE_ON_CLOSE` and `FILE_DISPOSITION_INFORMATION`, with child-create rejection while pending, `STATUS_DIRECTORY_NOT_EMPTY` rejection for non-empty directories, and verified core, client, server, and loopback coverage while broader create-option semantics remain backlog.
- Added the sixteenth Milestone 3 vertical slice: SMB 2.0.2 bounded directory rename handling through `FILE_RENAME_INFORMATION` on directory opens, with missing-destination moves, subtree-open rejection, declared-allocation preservation for moved child paths, and verified client, server, and loopback coverage while broader rename semantics remain backlog.
- Added the seventeenth Milestone 3 vertical slice: SMB 2.0.2 bounded unsigned synchronous `CANCEL` handling with fixed-body request codecs, client cancel-header creation that reuses the target `MessageId` without consuming an extra credit or sequence number, server cancellation of accepted pending requests, `STATUS_CANCELLED` target responses, and verified core, client, server, and loopback coverage while signed, async, and compounded cancel semantics remain backlog.
- Added the eighteenth Milestone 3 vertical slice: SMB 2.0.2 bounded file overwrite-disposition handling for `FILE_SUPERSEDE`, `FILE_OVERWRITE`, and `FILE_OVERWRITE_IF`, with required `DELETE` access validation for supersede, truncation and create-action reporting, declared-allocation reset on replacement, and verified core, client, server, and loopback coverage while broader create semantics remain backlog.
- Added the nineteenth Milestone 3 vertical slice: SMB 2.0.2 bounded `FILE_BASIC_INFORMATION` timestamp handling for creation, last-access, last-write, and `ChangeTime`-only updates, with explicit rejection of unsupported sticky `-1` and `-2` timestamp directives, local-filesystem fallback of `ChangeTime` onto the last-write path, and verified core, client, server, and loopback coverage while handle-persistent timestamp semantics remain backlog.
- Added the twentieth Milestone 3 vertical slice: SMB 2.0.2 bounded create-time file-attribute handling for create and overwrite paths, with local-share Hidden-attribute application, `STATUS_CANNOT_DELETE` rejection for new read-only `FILE_DELETE_ON_CLOSE` requests, and verified client, server, and loopback coverage while broader create-state semantics remain backlog.
- Added the twenty-first Milestone 3 vertical slice: SMB 2.0.2 bounded read-only delete rejection for `FILE_DELETE_ON_CLOSE` and `FILE_DISPOSITION_INFORMATION`, covering existing read-only files and directories plus new read-only directory creates, with verified server and loopback coverage while broader create-state semantics remain backlog.
- Added the twenty-second Milestone 3 vertical slice: SMB 2.0.2 bounded handle-persistent `FILE_BASIC_INFORMATION` timestamp semantics for `-1` / `-2` access, write, and change-time directives, explicit `ChangeTime` handling without the prior last-write fallback, tracked timestamp cleanup on delete, and verified core, client, server, and loopback coverage for timestamp suppression and resumption across read, write, allocation, and end-of-file mutations while broader create-state semantics remain backlog.
- Added the twenty-third Milestone 3 vertical slice: SMB 2.0.2 bounded async `CHANGE_NOTIFY` handling with SMB2 async header codecs, interim `STATUS_PENDING` and final async response flow, pending directory-watch lifecycle, create-event completion, `STATUS_NOTIFY_ENUM_DIR` overflow, async `CANCEL` integration for accepted pending notify requests, client-side notify-entry decoding, and verified core, client, server, and loopback coverage while broader notify filters, richer event coverage, signed cancel, and asynchronous compounding remain backlog.
