# Coverage Matrix

| dialect | area | command or capability | server status | client status | advertised | implemented | test suite name | descriptor count | descriptor skipped count | last samba verification date | last windows verification date | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SMB1/CIFS | negotiate | dialect negotiation | backlog | backlog | false | false | OpenCIFS.Core.Tests.Console | 0 | 0 | n/a | n/a | SMB1 remains disabled by default even after implementation. |
| SMB1/CIFS | session | session setup and logoff | backlog | backlog | false | false | OpenCIFS.Server.Tests.Console | 0 | 0 | n/a | n/a | Not yet implemented. |
| SMB1/CIFS | tree | tree connect and disconnect | backlog | backlog | false | false | OpenCIFS.Server.Tests.Console | 0 | 0 | n/a | n/a | Not yet implemented. |
| SMB1/CIFS | file io | create, read, write, close | backlog | backlog | false | false | OpenCIFS.Server.Tests.Console | 0 | 0 | n/a | n/a | Not yet implemented. |
| SMB1/CIFS | metadata | query, set, enumerate | backlog | backlog | false | false | OpenCIFS.Server.Tests.Console | 0 | 0 | n/a | n/a | Not yet implemented. |
| SMB1/CIFS | locking | locking and oplock compatibility | backlog | backlog | false | false | OpenCIFS.Server.Tests.Console | 0 | 0 | n/a | n/a | Not yet implemented. |
| SMB1/CIFS | security | ntlmv2 and spnego | backlog | backlog | false | false | OpenCIFS.Core.Tests.Console | 0 | 0 | n/a | n/a | SMB1 authentication work is pending. |
| SMB1/CIFS | transport | netbios session service | backlog | backlog | false | false | OpenCIFS.Core.Tests.Console | 0 | 0 | n/a | n/a | Not yet implemented. |
| SMB 2.0.2 | negotiate | dialect negotiation | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 2 | 0 | 2026-04-30 | 2026-04-30 | Loopback negotiate coverage selects SMB 2.0.2 only; codec and policy validation also run in the core, client, and server shared suites. Docker-backed Samba 4.17.12 smoke verified negotiate in both directions on 2026-04-30, and the native Windows mapped-drive smoke verified negotiate against `Sample.OpenCifsServer` on 2026-04-30, while full dialect advertisement stays off until broader SMB 2.0.2 command coverage exists. |
| SMB 2.0.2 | session | session setup, logoff, echo keepalive, and bounded cancel | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 5 | 0 | 2026-04-30 | 2026-04-30 | Two-leg SPNEGO-wrapped NTLMv2 session setup, success logoff, authenticated-session `ECHO` keepalive, bad-credential rejection, and a bounded `CANCEL` slice for accepted pending requests, including async `CHANGE_NOTIFY` cancellation, are covered in the core, client, server, and loopback shared suites. Docker-backed Samba 4.17.12 smoke verified authenticated session setup in both directions on 2026-04-30, and the native Windows mapped-drive smoke verified SPNEGO-wrapped NTLMv2 session setup against `Sample.OpenCifsServer` on 2026-04-30. Connection-level unsigned `SessionId=0` echo handling plus signed and compounded cancel semantics remain backlog. |
| SMB 2.0.2 | tree | tree connect and disconnect | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 5 | 0 | 2026-04-30 | 2026-04-30 | Tree connect, tree disconnect, unauthenticated access denial, unknown-share rejection, explicit multi-share routing, and suppression of the implicit legacy options share when explicit registrations exist are covered in the core, client, server, and loopback shared suites. Docker-backed Samba 4.17.12 smoke verified tree connect and disconnect in both directions on 2026-04-30, and the native Windows mapped-drive smoke verified tree connect on 2026-04-30 while broader SMB 2.0.2 command coverage continues in the file-I/O, metadata, state, and compounding rows below. |
| SMB 2.0.2 | file io | create, read, write, close, flush | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 8 | 0 | 2026-04-30 | 2026-04-30 | Temp-share loopback coverage verifies create, write, flush, read, close, EOF behavior, delete-on-close cleanup, delete-pending reopen rejection, bounded `FILE_DIRECTORY_FILE` `FILE_CREATE` / `FILE_OPEN_IF` directory creation and reopen semantics, empty-directory `FILE_DELETE_ON_CLOSE` child-create rejection and last-close removal, bounded `FILE_SUPERSEDE` / `FILE_OVERWRITE` / `FILE_OVERWRITE_IF` handling with truncation, create-action reporting, and declared-allocation reset on replacement, bounded create-time Hidden-attribute application on create and overwrite paths, `STATUS_CANNOT_DELETE` rejection for new read-only `FILE_DELETE_ON_CLOSE` requests, and read-only delete-on-close rejection for existing file and directory targets. Core, client, and server shared suites also cover codecs, validators, create-request attribute preservation, open tracking, stale handles, explicit invalid session/tree/open identifier rejection across the bounded file-I/O surface, collisions, share-access conflicts, path rejection, non-empty directory `STATUS_DIRECTORY_NOT_EMPTY` rejection for delete-pending requests, directory-file collision versus `STATUS_NOT_A_DIRECTORY` behavior, missing-file `FILE_OVERWRITE` rejection, and required `DELETE` access validation for `FILE_SUPERSEDE`. Docker-backed Samba 4.17.12 smoke verified create, write, flush, read, close, rename, delete, and directory cleanup in both directions on 2026-04-30, and the native Windows mapped-drive smoke verified directory create, file write, file read, file rename, file delete, and empty-directory delete on 2026-04-30. Related-operation compounding and the remaining SMB 2.0.2 command backlog stay unchecked. |
| SMB 2.0.2 | metadata | query info, set info, query directory | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 5 | 0 | 2026-04-30 | 2026-04-30 | Shared core, server, and loopback suites cover `QUERY_INFO` for basic, standard, name, internal, network-open, all-information, and bounded filesystem-size payloads, `SET_INFO` for basic creation, explicit `ChangeTime`, handle-persistent `FILE_BASIC_INFORMATION` `-1` / `-2` access, write, and change-time directives, attribute updates, attribute-only read-only clearing that still rejects file-read data access, allocation size, end-of-file mutation, disposition handling, explicit `STATUS_CANNOT_DELETE` rejection for `FILE_DISPOSITION_INFORMATION` delete-pending requests on read-only files and directories, file rename, and bounded directory rename through `FILE_RENAME_INFORMATION` on directory opens with subtree-open rejection plus moved-child allocation-state preservation, alongside bounded `QUERY_DIRECTORY` coverage for existing-directory opens, `FILE_DIRECTORY_INFORMATION`, `FILE_FULL_DIR_INFORMATION`, `FILE_BOTH_DIR_INFORMATION`, search-pattern filtering, restart and resume enumeration state, exhaustion or error statuses, and explicit invalid session/tree/open identifier rejection across the bounded metadata surface. Loopback coverage verifies automatic timestamp suppression and resumption through read and write activity, and server coverage extends that verification through allocation and end-of-file mutation paths. Docker-backed Samba 4.17.12 smoke verified bounded metadata query, filesystem-size query, directory enumeration, rename, and delete flows on 2026-04-30, and the native Windows mapped-drive smoke verified hidden and read-only attribute mutation, read-only clearing, bounded EOF truncation, existing-directory opens, directory rename, and `FILE_BOTH_DIR_INFORMATION` enumeration on 2026-04-30. Broader metadata backlog still remains outside this slice. |
| SMB 2.0.2 | notifications | bounded async change notify | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 2 | 0 | 2026-04-30 | 2026-04-30 | Core, client, server, and loopback shared suites cover SMB2 async header codecs and validators, `CHANGE_NOTIFY` request and response bodies, interim `STATUS_PENDING` plus final async response handling, pending request tracking, create-event completion, same-directory rename and delete completion, non-recursive nested-create suppression plus cancellation, `STATUS_NOTIFY_ENUM_DIR` overflow, client-side relative-path decoding, and async `CANCEL` integration for a watched directory open. Native Windows mapped-drive smoke verified `FileSystemWatcher` nested-create behavior with and without subtree monitoring plus watcher-visible rename delivery on 2026-04-30. Broader notify filters, deeper event coverage, and asynchronous compounding remain backlog. |
| SMB 2.0.2 | locking | bounded byte-range lock, unlock, and exclusive oplock break | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 2 | 0 | 2026-04-30 | 2026-04-30 | Core, client, server, and loopback shared suites cover SMB2 `LOCK` request and response codecs, shared and exclusive byte-range ownership tied to opens, unlock handling, conflict rejection for competing lock requests and conflicting read or write operations, lock release on close, plus signed exclusive `OPLOCK_BREAK` notification and acknowledgment codecs, conflict-triggered downgrade handling, invalid notification and acknowledgment rejection, and rejection when a conflicting open prevents a new exclusive grant. Live direct-TCP client coverage verifies cross-session lock-conflict rejection and exclusive oplock-break completion, and the native Windows mapped-drive smoke verified byte-range lock-conflict rejection on 2026-04-30. Async cancellation of long-running lock requests plus broader oplock or lease behavior remain backlog. |
| SMB 2.0.2 | pass-through | bounded FSCTL request and response handling | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 1 | 0 | 2026-04-30 | n/a | Shared core, server, and loopback suites cover generic SMB2 IOCTL codecs and validators, bounded `FSCTL_VALIDATE_NEGOTIATE_INFO` request and response handling, bounded `FSCTL_SRV_ENUMERATE_SNAPSHOTS` request and response handling with empty `SRV_SNAPSHOT_ARRAY` success results, unsupported wildcard-file-id connection-scoped FSCTL handling, stale-handle and malformed-shape rejection, compounded payload trimming, and header-complete loopback coverage. Docker-backed Samba 4.17.12 smoke exercised `FSCTL_VALIDATE_NEGOTIATE_INFO` against `Sample.OpenCifsServer` on 2026-04-30. Non-FSCTL device-control behavior, broader FSCTL implementations, and async command backlog remain unchecked. |
| SMB 2.0.2 | security | signing and session security | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 6 | 0 | 2026-04-30 | 2026-04-30 | Shared client, server, and loopback suites now cover authenticated SMB2 request signing, signed-response validation, signed cancel headers, and rejection of missing or tampered signatures on the managed client and server path. Live direct-TCP client coverage runs through the same post-authenticated signing path, while the bounded real-client `smbprotocol`, Docker-backed Samba 4.17.12, and native Windows mapped-drive smokes were all rerun with signing required on 2026-04-30. Session-setup signing, SMB 3.x signing negotiation, and encryption-selection behavior remain backlog outside this SMB 2.0.2 slice. |
| SMB 2.0.2 | state | credit accounting, message-id window, and bounded cancel reuse | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 3 | 0 | n/a | n/a | Header-wrapped loopback coverage verifies a stable SMB 2.0.2 credit window across negotiate, session, tree, and file-I/O requests plus a bounded cancel path that reuses the target `MessageId` without consuming an extra credit or sequence number. Client and server shared suites also cover reserved `CreditCharge=0` handling, explicit local credit exhaustion rejection, out-of-order response completion, cancel-header creation, cancellation of accepted pending requests, credit clamping, and rejection of invalid flags, malformed response headers, and reused or unknown message identifiers. |
| SMB 2.0.2 | compounding | unrelated packets plus bounded related synchronous tree, file, metadata, locking, and open-scoped IOCTL chains | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 2 | 0 | n/a | n/a | Core, client, server, and loopback shared suites cover packet framing, zero-padding trimming, header application, and server dispatch for unrelated negotiate, session, tree, create, read, write, flush, close, echo, and bounded unsupported IOCTL message bodies, plus bounded `SMB2_FLAGS_RELATED_OPERATIONS` chains that carry `SessionId`, generated `TreeId`, and generated `FileId` through synchronous `tree connect -> create -> set info -> query info -> lock -> ioctl -> close -> tree disconnect` flows with mixed-style rejection, propagated related-create failure across later metadata, locking, IOCTL, and close legs, and rejection of related chains that begin outside the supported synchronous tree, file, metadata, locking, and open-scoped IOCTL surface. The client and loopback suites now also cover realistic related `create -> write -> flush -> close`, `create -> query info(FILE_ALL_INFORMATION) -> close`, and `open -> read -> close` chains, including signed related-response preservation and missing-path create-failure propagation. Broader related-chain semantics and asynchronous compounding remain backlog. |
| SMB 2.0.2 | client surface | direct-tcp low-level and high-level common operations | implemented | implemented | false | true | OpenCIFS.Client.Tests.Console | 18 | 0 | 2026-04-30 | n/a | Client shared suites now cover the non-preview `OpenCifsClientConnection` advanced surface plus the aligned primary `OpenCifsClientBuilder` -> `OpenCifsClient` -> `OpenCifsShareSession` path over live-listener `OpenCifsDirectTcpServer` runs, while the older `OpenCifsClientFacade` remains covered as a secondary managed surface. The low-level connection coverage verifies direct-TCP connect, negotiate, authenticate, tree connect, create or open, read, write, flush, query info, set end-of-file, file rename, query directory, bounded async `CHANGE_NOTIFY` completion for create, rename, and delete, non-recursive nested-create cancellation, signed exclusive oplock-break wait-and-ack handling, file and empty-directory delete, close, tree disconnect, bad-credential rejection, stale-handle rejection, non-empty-directory delete rejection, shared-read success across concurrent sessions, conflicting share-access rejection, cross-session byte-range lock-conflict rejection, rejection of a second open that cannot receive an exclusive oplock, durable byte-range lock preservation across transport disconnect and reconnect, bounded SMB 2.1 lease-backed durable reconnect success plus competing-open downgrade to `SMB2_LEASE_NONE`, bounded realistic related `create -> query -> close`, `open -> read -> close`, and `create -> write -> flush -> close` helper flows, explicit `OpenCifsStatusException` propagation with SMB2 command, NTSTATUS, and normalized `OpenCifsErrorCategory`, representative `OpenCifsClientStateException` plus `OpenCifsClientProtocolException` coverage for disconnected tree use and malformed response-header handling, and bounded advanced/raw `Try...Async` result-envelope coverage across lifecycle, compound, open, I/O, query, set, notify, close, and disconnect flows while preserving local argument-validation throws. The aligned primary client coverage verifies immutable settings, authenticated connect or echo, share-scoped directory create, file write or read, directory enumeration, metadata query, rename, file or empty-directory delete, byte-range lock acquire or release, explicit disconnect, rejection of share-session use after disconnect, the dedicated high-level sample file that avoids low-level primitives, and the bounded non-throwing `Try...Async` result-envelope path across connect, share open, grouped file/directory/metadata/lock success flows, missing-file and non-empty-directory failure envelopes, and preserved local argument-validation throws. The bounded primary file and metadata paths now also opportunistically route small direct-TCP reads, writes, and `FILE_ALL_INFORMATION` queries through those realistic compound helpers. Docker-backed Samba 4.17.12 smoke additionally validates `OpenCifsClientConnection` against Samba server for negotiate, authenticate, tree connect, nested-directory create, write, flush, read, `FILE_STANDARD_INFORMATION` query, bounded last-write mutation, bounded EOF truncation, directory enumeration before and after directory rename, non-empty-directory delete rejection, deleted-path reopen rejection, file and directory delete, bounded lock-conflict rejection, and interim async `STATUS_PENDING` handling on the direct-TCP response path. These managed direct-TCP client surfaces now also exercise shared multi-client direct-TCP server state for async response delivery, but they do not change dialect advertisement or the broader Windows-server interop backlog. |
| cross-dialect | server surface | share registration, lifecycle control, callbacks, and share introspection | implemented | n/a | false | true | OpenCIFS.Server.Tests.Console | 13 | 0 | n/a | n/a | Shared server suites now cover host-builder and primary builder share registration, duplicate-share rejection, suppression of the implicit legacy options share when explicit shares are registered, immutable `OpenCifsServerSettings`, the clearer `OpenCifsServerBuilder -> OpenCifsServer -> OpenCifsServerApplication` runtime handoff, managed application lifecycle control, bounded `TryRunAsync` / `TryStartAsync` / `TryStopAsync` result-envelope companions on `OpenCifsServerApplication`, typed callback hooks, immutable `OpenCifsServerShareInfo` snapshots returned by `OpenCifsServerBuilder`, `OpenCifsServer`, `OpenCifsServerHostBuilder`, `OpenCifsServerHost`, and `OpenCifsServerApplication` for explicit-share and implicit-options-share introspection plus the synthetic `IPC$` snapshot when bounded named-pipe endpoints are registered, and representative typed `OpenCifsServerConfigurationException` plus `OpenCifsServerStateException` coverage for duplicate-share, no-common-dialect, double-start, bind-conflict, and disposed-lifecycle failures on the documented server surface. README smoke, package smoke, and the published-sample smoke now also assert the documented server/runtime surface and share-introspection path. This row remains server-side configuration coverage; remote share browsing over `IPC$` or `srvsvc` is tracked separately. |
| SMB 2.0.2 | durability | bounded durable handle reconnect | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 3 | 0 | n/a | n/a | Core, client, server, and loopback shared suites cover SMB2 durable create and reconnect context codecs and validators, bounded `DHnQ` and `DHnC` request and response handling, shared-state persistent and volatile file ID allocation, durable batch-oplock request and grant, detached-open survival across abrupt transport disconnect, detached byte-range lock preservation while the durable open is offline, conflicting read and lock rejection while detached and after reconnect, reconnect on a fresh managed direct-TCP connection, restored unlock ownership after reconnect, mismatched reconnect rejection while preserving the detached open, and client rejection for reconnect attempts on nondurable handles. Broader durable-state carryover, Windows or Samba durable-handle interop, lease-based durability, and SMB 2.1 or 3.x reconnect semantics remain backlog. |
| SMB 2.1 | negotiate | dialect negotiation | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 4 | 0 | 2026-04-30 | 2026-04-30 | Shared client, server, core, and loopback suites now cover the managed offered-dialect list of `Smb2002` plus `Smb21`, compatible SMB 2.1 response acceptance, SMB 3.x rejection, SMB 2.0.2 clamp behavior, and SMB1 multi-protocol bootstrap bridging to the highest implemented SMB 2.x dialect with the SMB 2.1 client-GUID requirement enforced. The bounded Python real-client, Docker-backed Samba, and native Windows client smokes were rerun under SMB 2.1 on 2026-04-30. |
| SMB 2.1 | session | session setup, logoff, echo keepalive, and bounded cancel | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 5 | 0 | 2026-04-30 | 2026-04-30 | The existing SMB2 session surface now runs under negotiated SMB 2.1 without a split implementation: two-leg SPNEGO-wrapped NTLMv2 session setup, authenticated-session `ECHO`, success logoff, bad-credential rejection, and the bounded cancel path all remain covered in the shared and loopback suites, and the bounded Python real-client, Docker-backed Samba, and native Windows client smokes were rerun under SMB 2.1 on 2026-04-30. Session-setup signing, compounded cancel expansion, and broader SMB 3.x security behavior remain backlog. |
| SMB 2.1 | tree | tree connect and disconnect | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 5 | 0 | 2026-04-30 | 2026-04-30 | The existing authenticated tree-connect and tree-disconnect surface now runs under negotiated SMB 2.1, including unauthenticated access denial, unknown-share rejection, explicit multi-share routing, and suppression of the implicit legacy options share when explicit registrations exist. The bounded Python real-client, Docker-backed Samba, and native Windows client smokes were rerun under SMB 2.1 on 2026-04-30. |
| SMB 2.1 | file io | lockstep create, read, write, close, and flush without large MTU | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 8 | 0 | 2026-04-30 | 2026-04-30 | The bounded create, read, write, flush, close, share-mode, delete-on-close, directory-create, overwrite, and read-only delete-rejection surface is now verified under negotiated SMB 2.1 across shared suites, loopback flows, and the bounded Python real-client, Docker-backed Samba, and native Windows client smokes. The bounded large-MTU claim now lives in the separate multi-credit row below. |
| SMB 2.1 | file io | large MTU and multi-credit large read/write behavior | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 1 | 0 | 2026-04-30 | 2026-04-30 | Shared client, server, and loopback suites now cover SMB 2.1 `SMB2_GLOBAL_CAP_LARGE_MTU` negotiation, negotiated max read or write size tracking, client credit-window growth via authenticated `ECHO`, multi-credit `CreditCharge` request creation and validation, acceptance of compatible overcharged large-I/O requests, large-request rejection when the negotiated or local credit window is too small, and high-level facade chunking when the peer credit window is smaller than the total payload. Loopback coverage verifies a bounded multi-credit large-I/O round trip, `OpenCIFS.Client` to Samba verifies a `200000`-byte large-file write or read round trip over SMB 2.1, the native Windows mapped-drive smoke verifies a `200000`-byte large-file write or read plus hash check against `Sample.OpenCifsServer`, the Samba client smoke verifies `put` or `get` of a `200000`-byte file against `Sample.OpenCifsServer`, the Python `smbprotocol` smoke verifies the same `200000`-byte transfer through repeated signed real-client operations, and the timed `Release` soak now replays repeated SMB 2.1 large-file round trips alongside primary-client connection churn, durable reconnect, exclusive oplock breaks, and lease breaks. |
| SMB 2.1 | metadata | query info, set info, and query directory | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 5 | 0 | 2026-04-30 | 2026-04-30 | The bounded query-info, set-info, rename, disposition, and query-directory surface now runs under negotiated SMB 2.1, including attribute-only read-only clearing, bounded EOF truncation, filesystem-size query behavior, directory enumeration, file rename, and directory rename in shared suites, loopback flows, and the bounded Python real-client, Docker-backed Samba, and native Windows client smokes. Broader metadata backlog remains outside this slice. |
| SMB 2.1 | locking | bounded byte-range lock, unlock, and exclusive oplock break | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 2 | 0 | 2026-04-30 | 2026-04-30 | The bounded byte-range locking and signed exclusive oplock-break surface now runs under negotiated SMB 2.1, including conflict rejection, unlock behavior, cross-session direct-TCP lock-conflict coverage, and exclusive-break completion. Docker-backed Samba and the native Windows mapped-drive smoke both exercised bounded lock-conflict behavior under SMB 2.1 on 2026-04-30, and the timed `Release` soak now replays repeated exclusive oplock-break completion against the managed listener alongside large-file churn. Broader lock cancellation, lease-v2 behavior, and large-I/O-adjacent semantics remain backlog. |
| SMB 2.1 | locking | bounded lease acquisition and lease-break flows | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 2 | 0 | n/a | n/a | Shared core, server, client, direct-TCP, and loopback suites now cover SMB 2.1 `RqLs` create-context codecs and validators, read/write/handle lease grant handling, signed lease-break notification and acknowledgment codecs, managed client lease-state tracking plus wait-and-ack handling, downgrade-to-none behavior on conflicting opens, same-lease-key path-mismatch rejection, invalid acknowledgment rejection, and unexpected or unsigned notification rejection. The timed `Release` soak now also replays repeated lease-break completion against the managed listener alongside primary-client connection churn, durable reconnect, and large-file round trips. The broader SMB 2.1 lease-v2 surface and external lease-specific Samba or Windows interop remain backlog. |
| SMB 2.1 | security | signing and session security | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 6 | 0 | 2026-04-30 | 2026-04-30 | Authenticated SMB2 request signing, signed-response validation, signed cancel headers, and missing or tampered signature rejection now run under negotiated SMB 2.1 on the managed client and server path. The bounded Python real-client, Docker-backed Samba, and native Windows client smokes were rerun with signing required under SMB 2.1 on 2026-04-30. SMB 3.x signing negotiation and encryption-selection behavior remain backlog. |
| SMB 2.1 | durability | bounded durable reconnect for batch-oplock and lease-backed opens without multichannel-adjacent semantics | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 5 | 0 | n/a | n/a | The bounded durable create and reconnect context handling now runs under negotiated SMB 2.1 for both batch-oplock and lease-backed opens on the managed path. Shared client, server, and loopback suites cover detached-open survival across abrupt transport disconnect, detached byte-range lock preservation, reconnect on a fresh managed direct-TCP connection, restored unlock ownership, mismatched reconnect rejection, missing or wrong lease-context rejection for durable lease reconnect, same-client-guid lease reconnect success, reconnect-time lease-state restoration, and downgrade to `SMB2_LEASE_NONE` when a competing open exists while the durable lease-backed open is detached. Windows or Samba durable-handle interop, lease-v2, and multichannel-adjacent semantics remain backlog. |
| SMB 2.1 | durability | multichannel-adjacent semantics and broader durable reconnect state | backlog | backlog | false | false | OpenCIFS.Core.Tests.Console | 0 | 0 | n/a | n/a | Backlog row for the remaining SMB 2.1 reconnect and durability semantics outside the bounded direct-TCP slice. |
| SMB 3.0 | negotiate | opt-in dialect negotiation and bounded non-encryption capability flags | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 1 | 0 | n/a | n/a | Shared client, server, and loopback suites now cover opt-in advertisement of `Smb30` and `Smb302`, clamp-to-`Smb30` negotiation when the server caps below `Smb302`, SMB1 multi-protocol bridge selection of the highest advertised SMB2+ dialect, and bounded capability advertisement limited to the implemented `LargeMtu` and `Leasing` set. The verified scope requires the client to set `PreferEncryption = false` and the server to set `RequireEncryptionForSmb3 = false`; encryption capability and external SMB 3.x interop remain backlog. |
| SMB 3.0 | session | session setup and logoff | backlog | backlog | false | false | OpenCIFS.Server.Tests.Console | 0 | 0 | n/a | n/a | The current SMB 3.0 managed slice only covers negotiate plus AES-CMAC signing without a separate authenticated SMB 3.0 lifecycle claim. |
| SMB 3.0 | tree | tree connect and disconnect | backlog | backlog | false | false | OpenCIFS.Server.Tests.Console | 0 | 0 | n/a | n/a | The current SMB 3.0 managed slice only covers negotiate plus AES-CMAC signing without a separate authenticated SMB 3.0 tree-lifecycle claim. |
| SMB 3.0 | file io | encrypted file operations | backlog | backlog | false | false | OpenCIFS.Server.Tests.Console | 0 | 0 | n/a | n/a | Bounded encrypted file-I/O claims begin at SMB 3.0.2 in the current implementation. |
| SMB 3.0 | metadata | query info, set info, query directory | backlog | backlog | false | false | OpenCIFS.Server.Tests.Console | 0 | 0 | n/a | n/a | Bounded encrypted metadata claims begin at SMB 3.0.2 in the current implementation. |
| SMB 3.0 | locking | lock, lease, durable handles | backlog | backlog | false | false | OpenCIFS.Server.Tests.Console | 0 | 0 | n/a | n/a | Bounded encrypted locking and durable reconnect claims begin at SMB 3.0.2 in the current implementation. |
| SMB 3.0 | security | opt-in AES-CMAC signing | implemented | implemented | false | true | OpenCIFS.Server.Tests.Console | 1 | 0 | n/a | n/a | Shared client and server suites now cover SMB 3.0 session-key-to-signing-key derivation plus authenticated request/response signing, validation, tamper rejection, and bounded managed secure-negotiate validation request/response matching with mismatch rejection on the direct-TCP path. External SMB 3.x signing interop and encryption remain backlog. |
| SMB 3.0 | durability | secure negotiate and reconnect | backlog | backlog | false | false | OpenCIFS.Core.Tests.Console | 0 | 0 | n/a | n/a | Bounded managed secure-negotiate validation now exists for SMB3 sessions on the direct-TCP path, but broader SMB 3.0 reconnect behavior is still unclaimed so this mixed durability row remains backlog. |
| SMB 3.0.2 | negotiate | encryption-capable dialect negotiation, bounded capability flags, and managed authenticated loopback lifecycle | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 4 | 0 | 2026-04-30 | 2026-04-30 | Shared client, server, and loopback suites now cover advertisement of `Smb30` and `Smb302`, negotiated selection of `Smb302`, the bounded non-encrypted compatibility slice when `PreferEncryption = false` and `RequireEncryptionForSmb3 = false`, the default encryption-capable negotiate path when both sides keep the encrypted SMB3 posture, SMB 3.1.1-style negotiate-request tolerance used by current Windows and Samba clients, bounded secure-negotiate validation with exact dialect-list matching plus client mismatch rejection on the managed SMB3 path, and capability advertisement limited to the implemented `LargeMtu`, `Leasing`, and `Encryption` set. The bounded Python real-client, Docker-backed Samba, and native Windows client smokes were all rerun under SMB 3.0.2 on 2026-04-30. |
| SMB 3.0.2 | session | encrypted session setup, echo, and logoff | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 2 | 0 | 2026-04-30 | 2026-04-30 | Shared client, server, and loopback suites now cover SMB 3.0.2 session setup with `EncryptData`, transform-wrapped post-authenticate requests and responses, encrypted `ECHO`, encrypted logoff, and bounded secure-negotiate validation on the managed SMB3 direct-TCP path. The bounded Python real-client, Docker-backed Samba, and native Windows client smokes were rerun under encryption-required SMB 3.0.2 on 2026-04-30. Broader SMB 3.0-only authenticated-session coverage remains backlog. |
| SMB 3.0.2 | tree | encrypted tree connect and disconnect | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 2 | 0 | 2026-04-30 | 2026-04-30 | Shared loopback coverage now exercises transform-wrapped SMB 3.0.2 tree connect and tree disconnect on the managed path, and the bounded Python real-client, Docker-backed Samba, and native Windows client smokes all reran tree lifecycle validation under encryption-required SMB 3.0.2 on 2026-04-30. |
| SMB 3.0.2 | file io | encrypted file operations and encrypted realistic compounding | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 4 | 0 | 2026-04-30 | 2026-04-30 | Shared loopback coverage now exercises transform-wrapped SMB 3.0.2 create, write, flush, read, close, encrypted realistic related `create -> write -> flush -> close`, and encrypted realistic related `open -> read -> close` chains on the managed path. The bounded Python real-client, Docker-backed Samba, and native Windows client smokes reran the same file-I/O surface under encryption-required SMB 3.0.2 on 2026-04-30, including a `200000`-byte large-file transfer. Broader external durable-handle v2 interop and multichannel remain backlog. |
| SMB 3.0.2 | metadata | encrypted query info, set info, query directory, rename, and delete | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 2 | 0 | 2026-04-30 | 2026-04-30 | The bounded Python real-client, Docker-backed Samba, and native Windows client smokes reran metadata query, bounded `FILE_BASIC_INFORMATION` mutation, bounded `FILE_END_OF_FILE_INFORMATION` truncation, directory enumeration, file rename, directory rename, file delete, nested-directory delete, and empty-directory cleanup under encryption-required SMB 3.0.2 on 2026-04-30. Managed loopback encrypted file-I/O and compounding coverage now exercises the same transform-wrapped request/response path the metadata surface uses, and the managed SMB3 path now also validates secure negotiate before normal tree/file workflows continue. Broader SMB 3.x metadata expansion remains backlog. |
| SMB 3.0.2 | locking | bounded encrypted lock-conflict, oplock, lease, and durable reconnect behavior | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 4 | 0 | 2026-04-30 | 2026-04-30 | Shared loopback coverage now exercises encrypted non-persistent durable-handle v2 reconnect with preserved byte-range lock state under SMB 3.0.2, while the existing managed locking, oplock, and lease state surfaces continue to run over the same authenticated session machinery. The bounded Docker-backed Samba and native Windows client smokes reran lock-conflict rejection under encryption-required SMB 3.0.2 on 2026-04-30. Lease-v2, continuous availability, and broader external oplock or lease interop remain backlog. |
| SMB 3.0.2 | security | AES-CMAC signing plus AES-128-CCM session encryption | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 4 | 0 | 2026-04-30 | 2026-04-30 | Shared core coverage now pins SMB3 transform-header framing plus AES-128-CCM encrypt/decrypt and tamper rejection, shared client and server suites cover SMB 3.0.2 session-key-to-signing-key derivation, AES-CMAC request/response validation, outbound encryption, inbound decryption, tampered encrypted-response rejection, and bounded secure-negotiate validation with direct-TCP mismatch rejection, and shared loopback coverage now verifies encrypted session, file-I/O, compounding, and durable reconnect flows. The bounded Python real-client, Docker-backed Samba, and native Windows client smokes all reran under encryption-required SMB 3.0.2 on 2026-04-30. |
| SMB 3.0.2 | durability | bounded encrypted non-persistent durable-handle v2 reconnect with secure negotiate validation | implemented | implemented | false | true | OpenCIFS.Interop.Tests.Console | 2 | 0 | n/a | n/a | Shared loopback coverage now exercises transform-wrapped SMB 3.0.2 non-persistent durable-handle v2 reconnect across host instances that share server state, including detached-open survival, preserved durable create GUID and bounded timeout state, reconnected read success, preserved persistent file identifier, fresh volatile file identifier allocation, preserved byte-range lock state, and the bounded managed secure-negotiate validation path that runs before normal SMB3 tree/file workflows continue. Continuous availability, persistent clustered handles, lease-v2, multichannel, and broader external durable-handle v2 interop remain backlog. |
| SMB 3.1.1 | negotiate | dialect negotiation | backlog | backlog | false | false | OpenCIFS.Core.Tests.Console | 0 | 0 | n/a | n/a | Bootstrap row. |
| SMB 3.1.1 | session | session setup and logoff | backlog | backlog | false | false | OpenCIFS.Server.Tests.Console | 0 | 0 | n/a | n/a | Bootstrap row. |
| SMB 3.1.1 | tree | tree connect and disconnect | backlog | backlog | false | false | OpenCIFS.Server.Tests.Console | 0 | 0 | n/a | n/a | Bootstrap row. |
| SMB 3.1.1 | file io | signed and encrypted file operations | backlog | backlog | false | false | OpenCIFS.Server.Tests.Console | 0 | 0 | n/a | n/a | Bootstrap row. |
| SMB 3.1.1 | metadata | query info, set info, query directory | backlog | backlog | false | false | OpenCIFS.Server.Tests.Console | 0 | 0 | n/a | n/a | Bootstrap row. |
| SMB 3.1.1 | locking | lock, lease, durable handles | backlog | backlog | false | false | OpenCIFS.Server.Tests.Console | 0 | 0 | n/a | n/a | Bootstrap row. |
| SMB 3.1.1 | security | preauth integrity, gmac signing, gcm/ccm encryption | backlog | backlog | false | false | OpenCIFS.Core.Tests.Console | 0 | 0 | n/a | n/a | Bootstrap row. |
| SMB 3.1.1 | negotiate contexts | full bounded preview slice with end-to-end dialect selection plus AES-GMAC signing plus AES-128-CCM and AES-128-GCM encryption | shared | shared | false | true | OpenCIFS.Client.Tests.Console | 16 | 0 | n/a | n/a | Shared core suite pins `PreauthIntegrityCapabilities`, `SigningCapabilities`, `EncryptionCapabilities`, and `NetnameNegotiateContext` round-trip plus malformed-input rejection, the `Smb2NegotiateContextList` codec serializes and parses 8-byte aligned typed context lists carried through both `Smb2NegotiateRequest` and `Smb2NegotiateResponse`, and tamper-rejection coverage asserts inflated entry-count rejection, zeroed hash-algorithm-count rejection, bytewise hash-algorithm tamper detection on a decoded preauth payload, and SMB 3.1.1 negotiate-request rejection when the carried negotiate-context offset is tampered below the fixed header. The `Smb311NegotiateContextSelector` static helper picks SHA-512 preauth hashing, prefers AES-GMAC > AES-CMAC > HMAC-SHA256 for signing, and prefers AES-128-GCM > AES-128-CCM for encryption. The bounded SMB 3.1.1 preview slice is now functional end-to-end: `OpenCifsClientBuilder.WithSmb311Preview()` and `OpenCifsServerBuilder.WithSmb311Preview()` are opt-in entry points (default off), `OpenCifsClientSession` allocates SHA-512 `PreauthIntegrityHashAccumulator` and emits four typed Preauth/Signing/Encryption/NETNAME contexts (signing offers `[AesGmac, AesCmac, HmacSha256]`, encryption offers `[Aes128Gcm, Aes128Ccm]`), both sides accumulate the preauth transcript across negotiate and session-setup, server-side `HandleNegotiate(...)` decodes typed client contexts and emits typed Preauth + Encryption + Signing response contexts with selected SHA-512, AES-128-GCM, and AES-GMAC, server's `GetMaximumImplementedDialect()` lifts to `Smb311` when opted in, both sides activate SMB 3.1.1 key derivation using the captured preauth hash and the negotiated cipher, `Smb3MessageTransform` switches between AES-128-CCM (11-byte nonce) and AES-128-GCM (12-byte nonce) per the negotiated cipher, and per-message AES-GMAC signing on both sides uses `Smb2SigningNonce.BuildSmb311GmacNonce(...)` to construct the 12-byte nonce per MS-SMB2 §3.1.4.1. Spec-strict client-side response validation rejects SMB 3.1.1 responses with no negotiate contexts, missing the mandatory preauth integrity context, selecting more than one hash algorithm, selecting an unsupported hash algorithm, selecting more than one cipher, or selecting an unsupported cipher. The bounded both-sides-opted-in live-listener test now explicitly asserts the negotiated dialect equals `Smb311` and completes a full encrypted authenticated write/read/disconnect cycle under derived SMB 3.1.1 keys with AES-GMAC signing and AES-128-GCM encryption. AES-256-GCM/CCM (gated on Kerberos session keys), NETNAME server-name verification, and external SMB 3.1.1 interop remain backlog so the dialect bit stays off outside the explicit opt-in preview. |
| SMB 3.1.1 | negotiate contexts | compression capabilities | backlog | backlog | false | false | OpenCIFS.Core.Tests.Console | 0 | 0 | n/a | n/a | `SMB2_COMPRESSION_CAPABILITIES` stays non-claimed until SMB 3.1.1 compression is fully implemented and covered. |
| SMB 3.1.1 | negotiate contexts | transport capabilities | backlog | backlog | false | false | OpenCIFS.Core.Tests.Console | 0 | 0 | n/a | n/a | `SMB2_TRANSPORT_CAPABILITIES` stays non-claimed until SMB over QUIC is fully implemented and covered. |
| SMB 3.1.1 | negotiate contexts | rdma transform capabilities | backlog | backlog | false | false | OpenCIFS.Core.Tests.Console | 0 | 0 | n/a | n/a | `SMB2_RDMA_TRANSFORM_CAPABILITIES` stays non-claimed until SMB Direct / RDMA is fully implemented and covered. |
| cross-dialect | foundation | binary primitives and endian codecs | shared | shared | false | true | OpenCIFS.Core.Tests.Console | 2 | 0 | n/a | n/a | Deterministic round-trip and malformed-input coverage for `LittleEndianReader` and `LittleEndianWriter`. |
| cross-dialect | foundation | protocol enums, status codes, and negotiate-context models | shared | shared | false | true | OpenCIFS.Core.Tests.Console | 4 | 0 | n/a | n/a | Covers dialect identifiers, wire-value stability, and negotiate-context model round-trips. |
| cross-dialect | foundation | direct tcp and netbios frame abstractions | shared | shared | false | true | OpenCIFS.Core.Tests.Console | 4 | 0 | n/a | n/a | Covers frame headers, validators, and frame-protocol malformed-input handling without advertising SMB1 transport support. The shared core mutation suite now also runs deterministic mutation corpuses for `DirectTcpFrameHeader` and representative direct-TCP frame payloads so malformed length and truncation paths stay bounded. |
| cross-dialect | foundation | smb1 and smb2/3 header codecs and validators | shared | shared | false | true | OpenCIFS.Core.Tests.Console | 3 | 0 | n/a | n/a | Covers serialization, parsing, alignment, flag legality, and malformed-input handling for shared SMB headers. The shared mutation suite now also runs deterministic corpuses across representative SMB2 negotiate, create, change-notify, IOCTL, lease-break, query-info, compounding, and `FILE_BOTH_DIR_INFORMATION` payload readers plus the SMB1 multi-protocol bootstrap path. |
| cross-dialect | foundation | fscc catalogs and shared lifetime state models | shared | shared | false | true | OpenCIFS.Core.Tests.Console | 4 | 0 | n/a | n/a | Covers FSCC information-class mappings plus connection/session/tree/open/request/credit/compound lifecycles. |
| SMB 3.1.1 | security | preauth hash accumulation foundation | shared | shared | false | true | OpenCIFS.Core.Tests.Console | 2 | 0 | n/a | n/a | Transcript hashing is implemented and tested without negotiate-time advertisement. |
| cross-dialect | security | ntlmv2 responses, MICs, and spnego token negotiation | shared | shared | false | true | OpenCIFS.Core.Tests.Console | 4 | 0 | n/a | n/a | Covers local MD4 and HMAC-MD5 support, Microsoft NTLMv2 vectors, MIC generation and verification, SPNEGO token round-trips, and malformed-token rejection without advertising Kerberos. |
| cross-dialect | security | signing algorithms, AEAD wrappers, and key derivation | shared | shared | false | true | OpenCIFS.Core.Tests.Console | 6 | 0 | n/a | n/a | Covers HMAC-SHA256, AES-CMAC, AES-GMAC, HKDF, SMB counter-mode and session subkey derivation, plus AES-CCM and AES-GCM wrappers with tamper detection. |
| cross-dialect | transport | `System.IO.Pipelines` framed connection scaffolding | shared | shared | false | true | OpenCIFS.Core.Tests.Console | 2 | 0 | n/a | n/a | Explicit in-memory read/write loop coverage exists without claiming socket- or dialect-level behavior. The shared server mutation suite now also drives malformed direct-TCP request bursts through the live listener and verifies bounded protocol-exception surfacing plus successful post-burst negotiate recovery on the same managed listener. |
| cross-dialect | dfs | bounded dfs referral codecs, server registration, and client get-referrals plus resolve-with-cache flows | implemented | implemented | true | true | OpenCIFS.Client.Tests.Console | 5 | 0 | n/a | n/a | Shared core suite pins `DfsReferralRequest` and `DfsReferralResponse` round-trip plus malformed-input rejection across the bounded version-2 entry shape. Shared server suite pins `OpenCifsServerHostBuilder.AddDfsReferral` null rejection, validation of zero-TTL referrals, duplicate-referral rejection, and clone-time field preservation. Shared client suite pins a live-listener flow where the managed server registers a bounded DFS referral, the managed client resolves the DFS path via `FSCTL_DFS_GET_REFERRALS` over `IPC$`, and a follow-up `ResolveDfsPathAsync` is served from the client referral cache. The client now also advertises `SMB2_GLOBAL_CAP_DFS` in its negotiate request to remain consistent with the managed DFS referral surface. Broader DFS namespace semantics, multi-target referrals, EX requests, and external Windows or Samba DFS interop remain backlog. |
| cross-dialect | named pipes | ipc$ transceive and bounded srvsvc share browsing/share inspection | implemented | implemented | false | true | eng/run-samba-interop.ps1 | 0 | 0 | 2026-04-30 | n/a | `OpenCIFS.Client` and the managed `OpenCIFS.Server` path now implement a bounded `IPC$` tree-connect, named-pipe open, public named-pipe transceive helpers, bounded public share-browse/share-info helpers, `FSCTL_PIPE_TRANSCEIVE`, and the DCE/RPC `srvsvc` `NetrShareEnum` / `NetrShareGetInfo` slice. Shared core coverage now pins the bounded request/response codecs and malformed-input rejection, the shared client suite pins managed echo-pipe transceive success plus missing-`IPC$`, missing-pipe, and missing-share failures, the shared server suite pins `IPC$` share exposure for both built-in and generic named-pipe endpoints plus duplicate endpoint rejection, the shared interop suite pins loopback pipe transceive plus loopback share-info through `IPC$`, `eng/run-test-console-smoke.ps1` now proves `OpenCIFS.TestClient shares`, `shareinfo`, and `pipe opencifs.echo ...` against `OpenCIFS.TestServer` end to end through OpenCIFS itself, and the Samba interop harness still proves real remote share browsing plus bounded share inspection across SMB 2.0.2, SMB 2.1, and SMB 3.0.2. Broader pipe semantics and DFS remain backlog. |
| cross-dialect | kerberos | native kerberos for smb | backlog | backlog | false | false | OpenCIFS.Interop.Tests.Console | 0 | 0 | n/a | n/a | Must remain non-advertised until fully implemented. |
| cross-dialect | interop | windows interoperability | implemented | backlog | false | true | eng/run-windows-client-interop.ps1 | 0 | 0 | 2026-04-30 | 2026-04-30 | Bounded native Windows mapped-drive smoke now runs through `eng/run-windows-client-interop.ps1`, with evidence in `artifacts/windows-client-interop/windows-client-smoke.json` and `artifacts/windows-client-interop/windows-client-environment.json`. The harness now automates the same mapped-drive flow under three merged dialect milestones, SMB 2.0.2, SMB 2.1, and encryption-required SMB 3.0.2, for `Sample.OpenCifsServer` mounted by the built-in Windows client, using isolated per-dialect UNC targets and longer custom-port mapping retries so back-to-back native runs stay stable. The verified scope covers negotiate, session setup, tree connect, directory create, nested-directory create, a `200000`-byte large-file write or read plus hash check, file write/read, hidden and read-only attribute mutation, read-only clearing, bounded EOF truncation, non-empty directory delete rejection, directory enumeration, file rename, directory rename, file delete, nested-directory delete, empty-directory cleanup, `FileSystemWatcher` nested-create behavior with and without subtree monitoring, watcher-visible rename delivery, share-access conflict rejection, and byte-range lock-conflict rejection. `eng/run-nightly-interop.ps1` now reruns the same three-dialect Windows matrix with a `262144`-byte payload and records the composed evidence in `artifacts/nightly-interop/nightly-interop.json`, while `OpenCIFS.Client` to Windows server coverage and durable-handle v2 remain backlog. |
| cross-dialect | interop | samba interoperability | implemented | implemented | false | true | eng/run-samba-interop.ps1 | 0 | 0 | 2026-04-30 | n/a | Bounded Docker-backed Samba 4.17.12 smoke now runs in both directions through `eng/run-samba-interop.ps1`, with exact version evidence recorded in `docs/interop-matrix.md`. The harness now automates both the `OpenCIFS.Client` to Samba path and the Samba client to `Sample.OpenCifsServer` path under three merged dialect milestones, SMB 2.0.2, SMB 2.1, and encryption-required SMB 3.0.2. The `OpenCIFS.Client` path verifies negotiate, session setup, tree connect, nested-directory create, a `200000`-byte large-file write or read round trip, read, write, flush, `FILE_STANDARD_INFORMATION` query, bounded last-write mutation, bounded EOF truncation, directory enumeration, file rename, directory rename, non-empty-directory delete rejection, deleted-path reopen rejection, file and directory delete, bounded lock-conflict rejection, automatic secure-negotiate validation on the SMB 3.0.2 lane, and encrypted SMB 3.0.2 file or metadata cleanup behavior against Samba server. The Samba client path verifies session setup, `FSCTL_VALIDATE_NEGOTIATE_INFO`, nested-directory create, `put` or `get` of a `200000`-byte file, rename, directory listing, non-empty-directory delete rejection, and cleanup against `Sample.OpenCifsServer`. `eng/run-nightly-interop.ps1` reruns both three-dialect Samba directions with a `262144`-byte payload and records the composed evidence in `artifacts/nightly-interop/nightly-interop.json`, while broader command-by-command Samba interop automation, Windows-server parity, and durable-handle v2 remain backlog. |


