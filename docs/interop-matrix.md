# Interop Matrix

| role | peer | target environment | dialect scope | smoke status | deep status | last verified date | evidence | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Sample.OpenCifsServer | Python `smbprotocol` client | Microsoft Windows 11 Pro 10.0.26200, Python 3.14.0, `smbprotocol` 1.16.1 | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | pass | pass | 2026-04-30 | `eng/run-real-client-interop.ps1`, `eng/run-nightly-interop.ps1`, `artifacts/real-client-interop/sample-server.config.txt`, `artifacts/real-client-interop/real-client-smoke.json`, `artifacts/nightly-interop/nightly-interop.json` | Bounded real-client smoke over a dynamically selected loopback direct-TCP port now runs with `require_signing=True`, a pinned SMB 2.0.2 or SMB 2.1 matrix, and an encryption-required SMB 3.0.2 lane. It verified a builder-backed local filesystem share driven through runtime sample-host overrides with negotiate, raw NTLMv2 session setup, tree connect, directory create, nested-directory create, a `200000`-byte large-file transfer through repeated signed or encrypted real-client operations, file write, flush, read, `FILE_STANDARD_INFORMATION` metadata query, bounded `FILE_BASIC_INFORMATION` Hidden-attribute and last-write mutation, bounded `FILE_END_OF_FILE_INFORMATION` truncation, read-only delete rejection, non-empty directory delete rejection, file rename, file delete, directory rename, nested-directory cleanup, empty-directory delete, and `FILE_FULL_DIRECTORY_INFORMATION` enumeration. The deeper nightly harness reruns the same three-dialect Python client matrix with a `262144`-byte payload and records the composed evidence in `artifacts/nightly-interop/nightly-interop.json`. Durable-handle v2 and broader SMB 3.x nightly coverage remain backlog. |
| Sample.OpenCifsServer | Samba client | Docker image `opencifs-samba-interop:bookworm`, Debian bookworm userspace, `smbclient` 4.17.12-Debian | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | pass | pass | 2026-04-30 | `eng/run-samba-interop.ps1`, `eng/run-nightly-interop.ps1`, `artifacts/samba-interop/sample-server.config.txt`, `artifacts/samba-interop/sample-server-samba-client.json`, `artifacts/samba-interop/sample-server-samba-client.log`, `artifacts/samba-interop/samba-versions.txt`, `artifacts/nightly-interop/nightly-interop.json` | Bounded Samba smoke over direct TCP against the runtime-configured sample host now runs against signing-required SMB 2.0.2 and SMB 2.1 listeners plus an encryption-required SMB 3.0.2 listener. It verified NTLMv2 session setup, tree connect, `FSCTL_VALIDATE_NEGOTIATE_INFO`, directory create, nested-directory create, `put` or `get` of a `200000`-byte large file, rename, directory enumeration, non-empty directory delete rejection, filesystem-size query behavior exercised by `smbclient`, file delete, nested-directory delete, directory delete, and clean directory cleanup. The deeper nightly harness reruns the same three-dialect Samba-client matrix with a `262144`-byte payload and records the composed evidence in `artifacts/nightly-interop/nightly-interop.json`. `eng/run-samba-peer-matrix.ps1` can now preserve per-peer Bookworm/Trixie artifacts under `artifacts/samba-peer-matrix`, but only the Bookworm evidence above is currently recorded as passing in this matrix. Durable-handle v2 and broader SMB 3.x nightly coverage remain backlog. |
| Sample.OpenCifsServer | Linux kernel CIFS client | Harness-ready; requires privileged Docker host with CIFS mount support | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | not run | not run | n/a | n/a | Harness target exists through `eng/run-linux-cifs-interop.ps1`, but no passing evidence has been checked in yet. The script builds a Debian `cifs-utils` image, starts `Sample.OpenCifsServer`, mounts the share with `mount.cifs` from a privileged container, pins each dialect lane, and records `artifacts/linux-cifs-interop/linux-cifs-interop.json` plus environment metadata when run. |
| Sample.OpenCifsServer | Windows client | Microsoft Windows 11 Pro 10.0.26200 built-in SMB client via `New-SmbMapping -TransportType TCP` | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | pass | pass | 2026-04-30 | `eng/run-windows-client-interop.ps1`, `eng/run-nightly-interop.ps1`, `artifacts/windows-client-interop/sample-server.config.txt`, `artifacts/windows-client-interop/windows-client-environment.json`, `artifacts/windows-client-interop/windows-client-smoke.json`, `artifacts/nightly-interop/nightly-interop.json` | Bounded native Windows mapped-drive smoke over a dynamically selected loopback direct-TCP port now runs against SMB 2.0.2-only, SMB 2.1-only, and encryption-required SMB 3.0.2 sample listeners, mounts the sample share through isolated per-dialect UNC targets on `\\localhost`, and verified negotiate, SPNEGO-wrapped NTLMv2 session setup, tree connect, directory create, nested-directory create, a `200000`-byte large-file write or read plus hash check, file write, file read, hidden and read-only attribute mutation, read-only clearing, bounded EOF truncation, non-empty directory delete rejection, `FILE_BOTH_DIR_INFORMATION` directory enumeration, file rename, directory rename, file delete, nested-directory delete, empty-directory delete, `FileSystemWatcher` nested-create behavior with and without subtree monitoring, watcher-visible rename delivery, share-access conflict rejection, and byte-range lock-conflict rejection. The harness now also waits out native Windows redirector teardown between back-to-back custom-port runs so the claimed three-dialect Windows matrix stays stable. The compatibility fixes behind these runs now also tolerate padded zero-length Windows create contexts, support `FILE_INTERNAL_INFORMATION` queries, preserve attribute-only read-only clearing without granting file-read data access, and accept SMB 3.1.1-style negotiate-request shapes plus bounded SMB3 durable or lease create hints on fresh opens. The deeper nightly harness reruns the same three-dialect Windows matrix with a `262144`-byte payload and records the composed evidence in `artifacts/nightly-interop/nightly-interop.json`. Durable-handle v2 and broader SMB 3.x nightly coverage remain backlog. |
| OpenCIFS.Client | Windows server | Harness-ready; requires pre-provisioned live Windows SMB share and credentials | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | not run | not run | n/a | n/a | Harness target exists through `eng/run-windows-server-interop.ps1`, but no passing evidence has been checked in yet. The script drives `OpenCIFS.TestClient` against an existing Windows SMB share over TCP 445 by default, pins each dialect lane, requires signing, prefers encryption for SMB 3.0.2, and records `artifacts/windows-server-interop/windows-server-interop.json` plus environment metadata when run. |
| OpenCIFS.Client | external DFS namespace | Harness-ready; requires a pre-provisioned writable Windows or Samba DFS namespace path and credentials | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | not run | not run | n/a | n/a | Harness target exists through `eng/run-dfs-interop.ps1`, but no passing evidence has been checked in yet. The script connects to the namespace share, fetches referrals, verifies referral-cache reuse on an immediate second resolution, reconnects to the redirected storage target using the resolved target server/share/path, and records `artifacts/dfs-interop/dfs-interop.json` plus environment metadata when run. The current bounded harness assumes the namespace and redirected storage target accept the same credentials and TCP port. |
| OpenCIFS.Client / OpenCIFS server | real SMB1/CIFS peers | Explicit non-claim for current release-gated interop scope | SMB1/CIFS | not run | not run | n/a | n/a | SMB1 codec and bootstrap coverage exists in `docs/coverage-matrix.md`, but real SMB1 client/server interoperability is intentionally outside the current release-gated interop claim. Any future real SMB1 peer testing must be isolated, opt-in, and separately evidenced. |
| OpenCIFS.TestClient | OpenCIFS.TestServer | Same-build separate processes on loopback direct TCP | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | pass | pass | 2026-05-13 | `eng/run-managed-interop.ps1`, `artifacts/managed-interop/managed-interop.json` | Bounded managed two-process interop runs `OpenCIFS.TestServer` and `OpenCIFS.TestClient` as separate executables, pins each dialect lane independently, requires signing, requires encryption for SMB 3.0.2, and verifies negotiate, NTLMv2 session setup, remote share browsing over `IPC$` and `srvsvc`, bounded share-info query, named-pipe transceive through `opencifs.echo`, tree connect, directory create, file write, directory enumeration, metadata query, file read, file rename, file download, file delete, directory delete, tree disconnect, and session disconnect. This is same-build managed-path evidence, not cross-version or independent-vendor interoperability evidence. |
| OpenCIFS.TestServer | OpenCIFS.TestClient | Same-build separate processes on loopback direct TCP | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | pass | pass | 2026-05-13 | `eng/run-managed-interop.ps1`, `artifacts/managed-interop/managed-interop.json` | Bounded managed two-process interop records the reciprocal server-side evidence for the same artifact-backed runs: `OpenCIFS.TestServer` exposes a filesystem share plus `IPC$`, `srvsvc`, and the bounded echo pipe, and records authenticated sessions, tree connects, creates, query-directory requests, and set-info requests while `OpenCIFS.TestClient` drives the dialect-pinned workflow. Cross-version managed client/server compatibility, Dockerized managed peers, and long-running managed two-process soak remain backlog. |
| OpenCIFS.Client | previous released OpenCIFS server package | Harness-ready; current project-reference client against a restored previous `OpenCIFS.Server` package on loopback direct TCP | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | not run | not run | n/a | n/a | Harness target now exists through `eng/run-cross-version-managed-interop.ps1`, but no checked-in pass evidence exists yet. The script restores a released `OpenCIFS.Server` package from a configurable package source, builds a temporary current-client consumer from local project references, runs the bounded two-process file-operation flow across the three release-gated dialects, and records `artifacts/cross-version-managed-interop/cross-version-managed-interop.json`. |
| OpenCIFS.Server | previous released OpenCIFS client package | Harness-ready; current project-reference server against a restored previous `OpenCIFS.Client` package on loopback direct TCP | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | not run | not run | n/a | n/a | Harness target now exists through `eng/run-cross-version-managed-interop.ps1`, but no checked-in pass evidence exists yet. The script restores a released `OpenCIFS.Client` package from a configurable package source, builds a temporary current-server consumer from local project references, runs the bounded two-process file-operation flow across the three release-gated dialects, and records `artifacts/cross-version-managed-interop/cross-version-managed-interop.json`. |
| OpenCIFS.Client | Samba server | Docker image `opencifs-samba-interop:bookworm`, Debian bookworm userspace, `smbd` 4.17.12-Debian | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | pass | pass | 2026-04-30 | `eng/run-samba-interop.ps1`, `eng/run-nightly-interop.ps1`, `artifacts/samba-interop/open-cifs-client-to-samba.json`, `artifacts/samba-interop/samba-server.log`, `artifacts/samba-interop/samba-versions.txt`, `artifacts/nightly-interop/nightly-interop.json` | Bounded Samba smoke now runs with `OpenCIFS.Client` pinned to SMB 2.0.2, SMB 2.1, and encryption-required SMB 3.0.2, with signing required and `server signing = mandatory`. It verified bounded remote share browsing over `IPC$` and `srvsvc`, with `BrowsedShareNames` including both the configured Samba share and `IPC$`, plus negotiate, NTLMv2 session setup, tree connect, nested-directory create, a `200000`-byte large-file write or read round trip, write, flush, read, `FILE_STANDARD_INFORMATION` query, bounded last-write mutation, bounded EOF truncation, `FILE_FULL_DIRECTORY_INFORMATION` enumeration before and after directory rename, non-empty-directory delete rejection, deleted-file and deleted-directory reopen rejection, file rename, file delete, nested-directory delete, directory rename, directory delete, bounded lock-conflict rejection, and automatic bounded secure-negotiate validation on the SMB 3.0.2 lane against Samba server. The direct-TCP client path also handled unsigned interim async `STATUS_PENDING` completion from Samba while still rejecting unsigned final async responses, and the SMB 3.0.2 lane completed the same file, metadata, cleanup, and remote-share-browse flow under AES-128-CCM session encryption. The deeper nightly harness reruns the same three-dialect `OpenCIFS.Client` to Samba matrix with a `262144`-byte payload and records the composed evidence in `artifacts/nightly-interop/nightly-interop.json`. `eng/run-samba-peer-matrix.ps1` can now preserve per-peer Bookworm/Trixie artifacts under `artifacts/samba-peer-matrix`, but only the Bookworm evidence above is currently recorded as passing in this matrix. Durable-handle v2 and broader SMB 3.x nightly coverage remain backlog. |
