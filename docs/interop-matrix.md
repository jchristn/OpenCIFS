# Interop Matrix

OpenCIFS `0.1.0` is alpha software. This matrix records the current bounded interoperability evidence and release-facing claims; it is not an exhaustive compatibility certification across SMB dialects, peers, platforms, or deployment environments.

| role | peer | target environment | dialect scope | smoke status | deep status | last verified date | evidence | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Sample.OpenCifsServer | Python `smbprotocol` client | Microsoft Windows 11 Pro 10.0.26200, Python 3.14.0, `smbprotocol` 1.16.1 | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | pass | pass | 2026-05-15 | `eng/run-real-client-interop.ps1`, `eng/run-nightly-interop.ps1`, `artifacts/real-client-interop/sample-server.config.txt`, `artifacts/real-client-interop/real-client-smoke.json`, `artifacts/nightly-interop/nightly-interop.json` | Bounded real-client smoke over a dynamically selected loopback direct-TCP port now runs with `require_signing=True`, a pinned SMB 2.0.2 or SMB 2.1 matrix, and an encryption-required SMB 3.0.2 lane. It verified a builder-backed local filesystem share driven through runtime sample-host overrides with negotiate, raw NTLMv2 session setup, tree connect, directory create, nested-directory create, a `200000`-byte large-file transfer through repeated signed or encrypted real-client operations, file write, flush, read, `FILE_STANDARD_INFORMATION` metadata query, bounded `FILE_BASIC_INFORMATION` Hidden-attribute and last-write mutation, bounded `FILE_END_OF_FILE_INFORMATION` truncation, read-only delete rejection, non-empty directory delete rejection, file rename, file delete, directory rename, nested-directory cleanup, empty-directory delete, and `FILE_FULL_DIRECTORY_INFORMATION` enumeration. The deeper nightly harness reruns the same three-dialect Python client matrix with a `262144`-byte payload and records the composed evidence in `artifacts/nightly-interop/nightly-interop.json`. When rerun, the per-dialect real-client artifact now also includes an `advanced_smb3` section with structured durable/oplock/lease outcome or skip data. Checked-in external durable-handle v2 pass evidence and broader SMB 3.x nightly evidence remain backlog. |
| Sample.OpenCifsServer | Samba client | Docker images `opencifs-samba-interop:bookworm` and `opencifs-samba-interop:trixie`; Debian bookworm or trixie userspace; `smbclient` 4.17.12-Debian or 4.22.8-Debian | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | pass | pass | 2026-05-15 | `eng/run-samba-interop.ps1`, `eng/run-samba-peer-matrix.ps1`, `eng/run-nightly-interop.ps1`, `artifacts/samba-interop/sample-server.config.txt`, `artifacts/samba-interop/sample-server-samba-client.json`, `artifacts/samba-interop/sample-server-samba-client.log`, `artifacts/samba-interop/samba-versions.txt`, `artifacts/samba-peer-matrix/debian-bookworm/sample-server-samba-client.json`, `artifacts/samba-peer-matrix/debian-bookworm/samba-versions.txt`, `artifacts/samba-peer-matrix/debian-trixie/sample-server-samba-client.json`, `artifacts/samba-peer-matrix/debian-trixie/samba-versions.txt`, `artifacts/nightly-interop/nightly-interop.json` | Bounded Samba smoke over direct TCP against the runtime-configured sample host now runs against signing-required SMB 2.0.2 and SMB 2.1 listeners plus an encryption-required SMB 3.0.2 listener. It verified NTLMv2 session setup, tree connect, `FSCTL_VALIDATE_NEGOTIATE_INFO`, directory create, nested-directory create, `put` or `get` of a `200000`-byte large file, rename, directory enumeration, non-empty directory delete rejection, filesystem-size query behavior exercised by `smbclient`, file delete, nested-directory delete, directory delete, and clean directory cleanup. The 2026-05-15 reruns refreshed the Bookworm evidence in `artifacts/samba-interop` and the passing Bookworm 4.17.12 plus Trixie 4.22.8 peer-matrix evidence under `artifacts/samba-peer-matrix`. When rerun, the Samba-client summaries now also record structured `advanced_smb3` skip reasons because `smbclient` does not surface reconnect tokens or break notifications through this bounded workflow. Checked-in external durable-handle v2 pass evidence and broader SMB 3.x nightly evidence remain backlog. |
| Sample.OpenCifsServer | Linux kernel CIFS client | Microsoft Windows 11 Pro 10.0.26200 host; WSL2 kernel 6.6.87.2-microsoft-standard-WSL2; Dockerized Debian bookworm; `mount.cifs` 7.0 | SMB 2.1, SMB 3.0.2 | pass | pass | 2026-05-15 | `eng/run-linux-cifs-interop.ps1`, `artifacts/linux-cifs-interop/linux-cifs-interop.json`, `artifacts/linux-cifs-interop/linux-cifs-environment.json` | The bounded Linux-kernel CIFS smoke now mounts `Sample.OpenCifsServer` from a privileged Debian bookworm container, pins SMB 2.1 and SMB 3.0.2, and verifies mount, directory create, nested-directory create, file write, read-back, a `200000`-byte large-file copy round trip, file rename, directory listing, non-empty-directory delete rejection, and cleanup. The harness now writes an LF-normalized client script into the artifact directory so Bash execution is stable on Windows hosts, and the sample server now exposes `IPC$` plus tolerates the Linux kernel rename payload shape and rename destination resolution used by this workflow. The artifact also now records an explicit skipped `smb2002` lane with a host-policy `skip_reason`. On this WSL2/Docker Desktop kernel, both `Sample.OpenCifsServer` and Samba `smbd` hit the same Linux kernel diagnostic, `vers=2.0 mount not permitted when legacy dialects disabled`, before any OpenCIFS traffic reaches the server, so checked-in pass evidence still intentionally covers SMB 2.1 and SMB 3.0.2 on this host. |
| Sample.OpenCifsServer | Windows client | Microsoft Windows 11 Pro 10.0.26200 built-in SMB client via `New-SmbMapping -TransportType TCP` | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | pass | pass | 2026-05-15 | `eng/run-windows-client-interop.ps1`, `eng/run-nightly-interop.ps1`, `artifacts/windows-client-interop/sample-server.config.txt`, `artifacts/windows-client-interop/windows-client-environment.json`, `artifacts/windows-client-interop/windows-client-smoke.json`, `artifacts/nightly-interop/nightly-interop.json` | Bounded native Windows mapped-drive smoke over a dynamically selected loopback direct-TCP port now runs against SMB 2.0.2-only, SMB 2.1-only, and encryption-required SMB 3.0.2 sample listeners, mounts the sample share through isolated per-dialect UNC targets on `\\localhost`, and verified negotiate, SPNEGO-wrapped NTLMv2 session setup, tree connect, directory create, nested-directory create, a `200000`-byte large-file write or read plus hash check, file write, file read, hidden and read-only attribute mutation, read-only clearing, bounded EOF truncation, non-empty directory delete rejection, `FILE_BOTH_DIR_INFORMATION` directory enumeration, file rename, directory rename, file delete, nested-directory delete, empty-directory delete, `FileSystemWatcher` nested-create behavior with and without subtree monitoring, watcher-visible rename delivery, share-access conflict rejection, and byte-range lock-conflict rejection. The harness now also waits out native Windows redirector teardown between back-to-back custom-port runs so the claimed three-dialect Windows matrix stays stable. The compatibility fixes behind these runs now also tolerate padded zero-length Windows create contexts, support `FILE_INTERNAL_INFORMATION` queries, preserve attribute-only read-only clearing without granting file-read data access, and accept SMB 3.1.1-style negotiate-request shapes plus bounded SMB3 durable or lease create hints on fresh opens. The deeper nightly harness reruns the same three-dialect Windows matrix with a `262144`-byte payload and records the composed evidence in `artifacts/nightly-interop/nightly-interop.json`. When rerun, the per-dialect workflow now also records an `advanced_smb3` section with structured durable/oplock/lease skip data on SMB 3.x lanes. Checked-in external durable-handle v2 pass evidence and broader SMB 3.x nightly evidence remain backlog. |
| OpenCIFS.Client | Windows server | Harness-ready; can use a pre-provisioned live Windows SMB share or the elevated local-share helper | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | not run | not run | n/a | n/a | Harness target exists through `eng/run-windows-server-interop.ps1`, and `eng/run-local-windows-server-interop.ps1` can provision a temporary local share plus account and delegate to that generic harness from an elevated PowerShell session. No passing evidence has been checked in yet. The generic script drives `OpenCIFS.TestClient` against an existing Windows SMB share over TCP 445 by default, pins each dialect lane, requires signing, prefers encryption for SMB 3.0.2, and records `artifacts/windows-server-interop/windows-server-interop.json` plus environment metadata when run. |
| OpenCIFS.Client | external DFS namespace | Dockerized Samba standalone DFS root and target on Debian bookworm; `smbd` 4.17.12-Debian; namespace share `namespace`; redirected target share `storage` | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | pass | pass | 2026-05-15 | `eng/run-dfs-interop.ps1`, `eng/run-samba-dfs-interop.ps1`, `artifacts/dfs-interop/dfs-interop.json`, `artifacts/dfs-interop/dfs-environment.json`, `artifacts/dfs-interop/samba-dfs-server.log`, `artifacts/dfs-interop/samba-dfs-versions.txt` | The bounded external DFS lane now provisions a writable Samba DFS namespace in Docker, fetches referrals through `OpenCIFS.Client`, verifies cache-backed repeat resolution against the same redirected target, reconnects to the redirected storage share, and exercises directory create, file write, enumerate, read, rename, renamed-file read, delete, and cleanup across SMB 2.0.2, SMB 2.1, and SMB 3.0.2. The current harness uses an explicit `GetDfsReferralsAsync` call before `ResolvePathAsync`, so the first resolution may already be served from the client referral cache; the checked-in artifact records that behavior and still verifies stable redirected target reuse on the immediate second resolution. The Samba wrapper is local-Docker automation over the generic DFS harness, and the generic harness still assumes the namespace and redirected storage target share the same credentials and TCP port. |
| OpenCIFS.Client / OpenCIFS server | real SMB1/CIFS peers | Explicit non-claim for current release-gated interop scope | SMB1/CIFS | not run | not run | n/a | n/a | SMB1 codec and bootstrap coverage exists in `docs/coverage-matrix.md`, but real SMB1 client/server interoperability is intentionally outside the current release-gated interop claim. Any future real SMB1 peer testing must be isolated, opt-in, and separately evidenced. |
| OpenCIFS.TestClient | OpenCIFS.TestServer | Same-build separate processes on loopback direct TCP | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | pass | pass | 2026-05-15 | `eng/run-managed-interop.ps1`, `artifacts/managed-interop/managed-interop.json` | Bounded managed two-process interop runs `OpenCIFS.TestServer` and `OpenCIFS.TestClient` as separate executables, pins each dialect lane independently, requires signing, requires encryption for SMB 3.0.2, and verifies negotiate, NTLMv2 session setup, remote share browsing over `IPC$` and `srvsvc`, bounded share-info query, named-pipe transceive through `opencifs.echo`, tree connect, directory create, file write, directory enumeration, metadata query, file read, file rename, file download, file delete, directory delete, tree disconnect, and session disconnect. This is same-build managed-path evidence; cross-version compatibility now has separate artifact-backed rows below, and independent-vendor interoperability evidence remains separate. |
| OpenCIFS.TestServer | OpenCIFS.TestClient | Same-build separate processes on loopback direct TCP | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | pass | pass | 2026-05-15 | `eng/run-managed-interop.ps1`, `artifacts/managed-interop/managed-interop.json` | Bounded managed two-process interop records the reciprocal server-side evidence for the same artifact-backed runs: `OpenCIFS.TestServer` exposes a filesystem share plus `IPC$`, `srvsvc`, and the bounded echo pipe, and records authenticated sessions, tree connects, creates, query-directory requests, and set-info requests while `OpenCIFS.TestClient` drives the dialect-pinned workflow. Released-binary cross-version evidence, Dockerized managed peers, and long-running managed two-process soak remain backlog. |
| OpenCIFS.Client | previous compatible OpenCIFS server package | Loopback direct TCP; current project-reference client against a local package feed built from commit `4af1f6d` | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | pass | pass | 2026-05-15 | `eng/run-cross-version-managed-interop.ps1`, `artifacts/cross-version-managed-interop/cross-version-managed-interop.json`, `artifacts/cross-version-package-feed/feed-metadata.json` | A 2026-05-15 local run built a previous compatible package feed from commit `4af1f6d`, then ran the current `OpenCIFS.Client` source build against that prior `OpenCIFS.Server` package across SMB 2.0.2, SMB 2.1, and SMB 3.0.2. The bounded two-process flow verified negotiate, NTLMv2 session setup, tree connect, directory create, file write, directory enumeration, file read, file rename, renamed-file read, file delete, directory delete, tree disconnect, and session disconnect. This is N-to-N-1 evidence from a local package feed, not yet a public released-package claim. |
| OpenCIFS.Server | previous compatible OpenCIFS client package | Loopback direct TCP; current project-reference server against a local package feed built from commit `4af1f6d` | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | pass | pass | 2026-05-15 | `eng/run-cross-version-managed-interop.ps1`, `artifacts/cross-version-managed-interop/cross-version-managed-interop.json`, `artifacts/cross-version-package-feed/feed-metadata.json` | The same 2026-05-15 local package feed run also verified the reciprocal N-to-N-1 direction: a prior `OpenCIFS.Client` package built from commit `4af1f6d` drove the current `OpenCIFS.Server` source build across SMB 2.0.2, SMB 2.1, and SMB 3.0.2 with the same bounded file-operation flow. This is package-backed compatibility evidence, but it still stops short of a public released-package compatibility claim. |
| OpenCIFS.Client | Samba server | Docker images `opencifs-samba-interop:bookworm` and `opencifs-samba-interop:trixie`; Debian bookworm or trixie userspace; `smbd` 4.17.12-Debian or 4.22.8-Debian | SMB 2.0.2, SMB 2.1, SMB 3.0.2 | pass | pass | 2026-05-15 | `eng/run-samba-interop.ps1`, `eng/run-samba-peer-matrix.ps1`, `eng/run-nightly-interop.ps1`, `artifacts/samba-interop/open-cifs-client-to-samba.json`, `artifacts/samba-interop/samba-server.log`, `artifacts/samba-interop/samba-versions.txt`, `artifacts/samba-peer-matrix/debian-bookworm/open-cifs-client-to-samba.json`, `artifacts/samba-peer-matrix/debian-bookworm/samba-versions.txt`, `artifacts/samba-peer-matrix/debian-trixie/open-cifs-client-to-samba.json`, `artifacts/samba-peer-matrix/debian-trixie/samba-versions.txt`, `artifacts/nightly-interop/nightly-interop.json` | Bounded Samba smoke now runs with `OpenCIFS.Client` pinned to SMB 2.0.2, SMB 2.1, and encryption-required SMB 3.0.2, with signing required and `server signing = mandatory`. It verified bounded remote share browsing over `IPC$` and `srvsvc`, with `BrowsedShareNames` including both the configured Samba share and `IPC$`, plus negotiate, NTLMv2 session setup, tree connect, nested-directory create, a `200000`-byte large-file write or read round trip, write, flush, read, `FILE_STANDARD_INFORMATION` query, bounded last-write mutation, bounded EOF truncation, `FILE_FULL_DIRECTORY_INFORMATION` enumeration before and after directory rename, non-empty-directory delete rejection, deleted-file and deleted-directory reopen rejection, file rename, file delete, nested-directory delete, directory rename, directory delete, bounded lock-conflict rejection, and automatic bounded secure-negotiate validation on the SMB 3.0.2 lane against Samba server. The 2026-05-15 reruns refreshed the passing Bookworm 4.17.12 evidence in `artifacts/samba-interop` and the passing Bookworm plus Trixie 4.22.8 peer-matrix evidence under `artifacts/samba-peer-matrix`. The same reruns also recorded concrete SMB 3.0.2 advanced outcomes: the bounded oplock-break and lease-break verification now pass on both Bookworm and Trixie, while durable-handle v2 is explicitly recorded as unsupported because the peer grants reconnect state but does not preserve detached competing-open lock semantics and rejects the reconnect create with `STATUS_OBJECT_NAME_NOT_FOUND`. Broader external durable-handle v2 pass evidence and broader SMB 3.x nightly evidence remain backlog. |

