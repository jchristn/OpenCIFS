# Interop Matrix

| role | peer | target environment | dialect scope | smoke status | deep status | last verified date | evidence | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Sample.OpenCifsServer | Python `smbprotocol` client | Microsoft Windows 11 Pro 10.0.26200, Python 3.14.0, `smbprotocol` 1.16.1 | SMB 2.1 | pass | not run | 2026-04-29 | `eng/run-real-client-interop.ps1`, `artifacts/real-client-interop/sample-server.config.txt`, `artifacts/real-client-interop/real-client-smoke.json` | Bounded real-client smoke over a dynamically selected loopback direct-TCP port now runs with `require_signing=True` and a pinned SMB 2.1 negotiate, and verified a builder-backed local filesystem share driven through runtime sample-host overrides with negotiate, raw NTLMv2 session setup, tree connect, directory create, nested-directory create, a `200000`-byte large-file transfer through repeated signed real-client operations, file write, flush, read, `FILE_STANDARD_INFORMATION` metadata query, bounded `FILE_BASIC_INFORMATION` Hidden-attribute and last-write mutation, bounded `FILE_END_OF_FILE_INFORMATION` truncation, read-only delete rejection, non-empty directory delete rejection, file rename, file delete, directory rename, nested-directory cleanup, empty-directory delete, and `FILE_FULL_DIRECTORY_INFORMATION` enumeration. The same workflow also records the effective tester-facing sample-server configuration and connection guidance before the listener starts. |
| Sample.OpenCifsServer | Samba client | Docker image `opencifs-samba-interop:bookworm`, Debian bookworm userspace, `smbclient` 4.17.12-Debian | SMB 2.1 | pass | not run | 2026-04-29 | `eng/run-samba-interop.ps1`, `artifacts/samba-interop/sample-server.config.txt`, `artifacts/samba-interop/sample-server-samba-client.json`, `artifacts/samba-interop/sample-server-samba-client.log`, `artifacts/samba-interop/samba-versions.txt` | Bounded Samba smoke over direct TCP against the runtime-configured sample host now runs against an SMB 2.1 signing-required sample listener and verified NTLMv2 session setup, tree connect, `FSCTL_VALIDATE_NEGOTIATE_INFO`, directory create, nested-directory create, `put` or `get` of a `200000`-byte large file, rename, directory enumeration, non-empty directory delete rejection, filesystem-size query behavior exercised by `smbclient`, file delete, nested-directory delete, directory delete, and clean directory cleanup. The same workflow records the effective sample-host configuration before the listener starts and preserves the exact Samba client version used for the run. |
| Sample.OpenCifsServer | Windows client | Microsoft Windows 11 Pro 10.0.26200 built-in SMB client via `New-SmbMapping -TransportType TCP` | SMB 2.1 | pass | not run | 2026-04-29 | `eng/run-windows-client-interop.ps1`, `artifacts/windows-client-interop/sample-server.config.txt`, `artifacts/windows-client-interop/windows-client-environment.json`, `artifacts/windows-client-interop/windows-client-smoke.json` | Bounded native Windows mapped-drive smoke over a dynamically selected loopback direct-TCP port now runs with signing required against an SMB 2.1-only sample listener, mounts the sample share through the isolated UNC target `\\localhost\share`, and verified negotiate, SPNEGO-wrapped NTLMv2 session setup, tree connect, directory create, nested-directory create, a `200000`-byte large-file write or read plus hash check, file write, file read, hidden and read-only attribute mutation, read-only clearing, bounded EOF truncation, non-empty directory delete rejection, `FILE_BOTH_DIR_INFORMATION` directory enumeration, file rename, directory rename, file delete, nested-directory delete, empty-directory delete, `FileSystemWatcher` nested-create behavior with and without subtree monitoring, watcher-visible rename delivery, share-access conflict rejection, and byte-range lock-conflict rejection. The compatibility fixes behind that run now also tolerate padded zero-length Windows create contexts, support `FILE_INTERNAL_INFORMATION` queries, and preserve attribute-only read-only clearing without granting file-read data access. The workflow records the effective port, mapped-drive state, Windows client environment, per-step command output, and final cleanup state. |
| OpenCIFS.Client | Windows server | Unassigned | SMB 2.0.2+ | not run | not run | n/a | n/a | Planned bootstrap target. |
| OpenCIFS.Client | Samba server | Docker image `opencifs-samba-interop:bookworm`, Debian bookworm userspace, `smbd` 4.17.12-Debian | SMB 2.1 | pass | not run | 2026-04-29 | `eng/run-samba-interop.ps1`, `artifacts/samba-interop/open-cifs-client-to-samba.json`, `artifacts/samba-interop/samba-server.log`, `artifacts/samba-interop/samba-versions.txt` | Bounded Samba smoke now runs with `OpenCIFS.Client` pinned to SMB 2.1, signing required, and `server signing = mandatory`, and verified negotiate, NTLMv2 session setup, tree connect, nested-directory create, a `200000`-byte multi-credit large-file write or read round trip, write, flush, read, `FILE_STANDARD_INFORMATION` query, bounded last-write mutation, bounded EOF truncation, `FILE_FULL_DIRECTORY_INFORMATION` enumeration before and after directory rename, non-empty-directory delete rejection, deleted-file and deleted-directory reopen rejection, file rename, file delete, nested-directory delete, directory rename, directory delete, and bounded lock-conflict rejection against Samba server. The direct-TCP client path also handled unsigned interim async `STATUS_PENDING` completion from Samba while still rejecting unsigned final async responses. |

